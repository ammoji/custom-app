# Testing findings — Android validation + full re-test (started May 30 2026)

Living list of bugs and critical-enhancement candidates surfaced
during the Android-validation + iOS re-test phase that started
May 30, before the 1-shop Ballabgarh pilot.

Each entry is sized for triage: severity, platform, scope, fix
approach, and current status. Critical bugs blocking pilot go to the
top; enhancements / nice-to-haves at the bottom.

---

## Bugs

### #1 — Android: cart bar overlapped by system nav, "View Cart" un-tappable

- **Severity:** **Pilot-blocker** for Android customers — they
  cannot proceed to checkout from a shop's menu screen, the home
  screen, the shop list, or search results. iOS unaffected.
- **Platform:** Android only. Reproducible on the Play Internal
  Testing install on Sudhir's new Android phone (May 30, 7:03 PM).
  Likely affects every Android phone with the standard
  gesture-navigation bar.
- **Symptom:** the green "N items · ₹X · View Cart ›" bar floats at
  the bottom of the screen but sits *behind* the Android system
  navigation pills (back / home / recent-apps). The "View Cart" tap
  target is intercepted by the OS, so customers cannot proceed to
  the Cart screen via this affordance.
- **Root cause:** `cartBar` styles use
  `position: 'absolute'; bottom: spacing.lg` — a flat distance from
  the screen bottom that does not account for the Android system
  navigation bar's height. iOS happens to clear by coincidence
  (home-indicator inset < `spacing.lg`); Android's nav bar is
  taller.
- **Scope:** 4 screens all use the identical anti-pattern.
  - `src/screens/HomeScreen.tsx` (~line 952)
  - `src/screens/ShopListScreen.tsx` (~line 223)
  - `src/screens/ShopDetailScreen.tsx` (~line 345)
  - `src/screens/SearchScreen.tsx` (~line 439)
  Plus: the `FlatList` / `ScrollView` content above each cart bar
  likely has bottom padding sized for `spacing.lg + cartBarHeight`
  only — that also needs `insets.bottom` added so the last list
  item doesn't end up hidden behind the floated bar.
- **Fix approach:** use `useSafeAreaInsets()` from
  `react-native-safe-area-context` (already a dependency; other
  screens use SafeAreaView from the same library). Add
  `insets.bottom` to both the cart bar's bottom offset and the
  scroll container's `paddingBottom`. OTA-safe — pure JS, no
  native module change, no permission change, no `app.json`
  change. Ships via `eas update --branch production`.
- **Status:** ✅ **SHIPPED in PR-NEXT-2** (May 31 2026). All four
  screens (`HomeScreen`, `ShopListScreen`, `ShopDetailScreen`,
  `SearchScreen`) now call `useSafeAreaInsets()` and apply
  `bottom: insets.bottom + spacing.sm` to the cart bar plus
  `paddingBottom: 120 + insets.bottom` to the scroll/list
  container. Hook placement follows code-discipline Rule 2 (with
  the other hooks, above any conditional early returns). 979/979
  tests stay green. OTA ship via `eas update --branch production`. **Cart + Checkout bottom CTA Android-overlap (different button from PR-NEXT-2's floating bar) → ✅ SHIPPED in PR-NEXT-HOTFIX-3** (June 1 2026): Sudhir's second smoke pass showed the Cart screen's "Proceed to Checkout · ₹X" button (and Checkout's "Pay" / "Place Order") still partly under the Android gesture-nav pill. Different button from PR-NEXT-2's territory — those four screens have a `position: 'absolute'` floating cart bar that escapes SafeAreaView's natural flow and so needs the manual `bottom: insets.bottom + spacing.sm` offset. CartScreen + CheckoutScreen's CTAs are in NORMAL flow inside `<SafeAreaView edges={['top']}>` — the `edges={['top']}` prop tells the library to skip the bottom inset entirely. Fix is the cleaner expression for in-flow content: `<SafeAreaView edges={['top', 'bottom']}>` on both `CartScreen.tsx:32, 48` and `CheckoutScreen.tsx:749, 765` (empty-cart branch + populated branch each). No style block changes, no new hooks, no `useSafeAreaInsets` import; the library handles the bottom padding from the device's real inset. iOS unchanged in practice (home-indicator inset already small enough that `spacing.lg` padding cleared it). 1155/1155 tests stay green. Pure OTA: `eas update --branch production --message "HOTFIX-3 Android cart + checkout bottom CTA"`. **Lesson for future bottom CTAs:** floating (`position: 'absolute'`) → `useSafeAreaInsets()` + `bottom: insets.bottom + spacing.X`; in-flow (sibling of scroll content) → `<SafeAreaView edges={['top', 'bottom']}>`. Never ship a bottom CTA with `edges={['top']}` alone.
- **Workaround until fix ships:** the in-screen cart-bar tap is
  blocked, but customers can still reach the Cart screen via the
  bottom tab bar (if present) or the back-then-forward navigation.
  For Sudhir's own testing during the fix window, ignore the
  cart-bar tap and navigate to Cart via whatever app-level
  navigation surface is reachable on the device.

---

### #2 — Cancel order: no push notification to shopkeeper

- **Severity:** **Pilot-blocker** — shopkeeper prepares the order, customer cancels, shopkeeper keeps preparing because they never got the signal. Wasted inventory + bad first impression on first cancellation.
- **Platform:** Both.
- **Symptom:** Customer cancels an order within the 2-min window; shopkeeper receives no push.
- **Root cause:** `updateOrderStatus` callable (functions/src/index.ts ~line 838) likely handles cancellation transitions but doesn't fan out a push to the shopkeeper. Need to verify the push trigger covers the `cancelled` transition and routes to shopkeeper, not just the customer.
- **Fix approach:** Extend the order-status push trigger to send to shopkeeper (and admin?) on cancellation. Tests pin the fan-out.
- **Status:** ✅ **SHIPPED in PR-NEXT-1** (May 31 2026). `sendOrderStatusPush` now fans out to shopkeeper (`pushToOwner`) + admin (`pushToAdmins`) on any→cancelled transition with explicit per-audience copy. Customer push remains via the existing `buildOrderStatusPushPlan` path.

### #3 — Notification deep-link: shopkeeper tap → home, not the order

- **Severity:** **High** (UX, bad at scale) — once shops get many orders, hunting for "which order was that notification?" wastes time per push.
- **Platform:** Both (push handler is JS).
- **Symptom:** Shopkeeper taps a new-order push notification; app opens to Home, not to the specific ShopOrderDetail.
- **Root cause:** PR 45.2 added push-tap deep-link handlers for `shop_pending_approval` and `delivery_request_pending` but not for the new-order notification.
- **Fix approach:** Extend the push-tap deep-link in `AuthBootstrap.tsx` (or wherever PR 45.2 put it) to handle the new-order notification type → navigate to `ShopOrderDetail` with the orderId. Mirror for the customer-side `order_status_changed` push → navigate to customer OrderDetail. Same for delivery → DeliveryOrderDetail.
- **Status:** ✅ **SHIPPED in PR-NEXT-1** (May 31 2026). `AuthBootstrap` now routes `new_order_for_shop`, `new_pickup_for_delivery`, `order_picked_up`, `order_cancelled`, `order_delivered`, and the legacy `order_status` push types to the appropriate detail screen. Audience derived from `useAuthStore` claims at tap time (shopOwner-with-matching-shopId precedence > admin > customer).

### #4 — Bulk "Mark N unavailable" silently fails — 0 updated, N skipped

- **Severity:** **High** — shopkeeper UX broken; can only update items one-by-one.
- **Platform:** Both (server bug).
- **Symptom:** Shopkeeper selects 3 menu items, taps "Mark 3 unavailable" → confirmation popup → confirm → "Updated with skips, 0 updated, 3 skipped (item may no longer exist)". But the items DO exist and are visible in the menu.
- **Root cause:** TBD. Likely a server-side bulk-update callable comparing wrong fields or using stale ids. Need to find the callable name + read its validator.
- **Fix approach:** Find the callable (probably `updateMenuItemsAvailability` or similar — grep for it). Read the per-item processing loop. Diagnose why each item is being skipped. Likely a field-mismatch (e.g., expecting `id` but getting `menuItemId`, or shop-id check failing).
- **Status:** ✅ **SHIPPED in PR-NEXT-4** (May 31 2026). Root cause confirmed during drafting: `bulkUpdateMenuAvailability` queried `db.collection('menuItems')` — a top-level collection that doesn't exist (per-shop menu items live at `shops/{shopId}/menu/{menuItemId}`). Every chunk-query returned empty → every requested ID got bucketed as `skippedCount` → user-facing message "item may no longer exist" misdirected diagnosis. Fixed by scoping the query to `shops/{shopId}/menu` via `FieldPath.documentId() in chunk`. The pre-PR `data.shopId === shopId` filter became dead code (subcollection scope guarantees it) and was dropped. Also: filter `deletedAt != null` in the chunk loop so a soft-deleted item (PR-NEXT-4 §C) doesn't get its `available` flag toggled. Bumped `updatedAt` write to `FieldValue.serverTimestamp()` (was `Date.now()`) to avoid the PR 48 §I mixed-type orderBy bug. **Smart bulk-action labels (flip-count, no-op buttons hidden) → ✅ SHIPPED in PR-NEXT-ENH-1** (June 1 2026): follow-up Sudhir surfaced while testing the PR-NEXT-4 fix — the bulk action bar always rendered both `Mark N unavailable` and `Mark N available` with the same N (the total selection size), so selecting 3 already-available items still showed `Mark 3 available` as a no-op option, and a mixed selection of 2-available + 1-unavailable showed both buttons with the wrong count on each. Pure helper `computeBulkAvailabilityCounts` (`@c:\Users\dahiy\grocery-mvp\src\utils\bulkAvailabilityCounts.ts`) returns `{ availableCount, unavailableCount }` — those drive the button labels and visibility. `ShopMenuScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\shop\ShopMenuScreen.tsx:200-214` + `:608-655`) now: (1) memoises the counts against `items` + `selectedIds`; (2) renders only the buttons whose flip-count is `> 0` (uniform selection → one full-width button; mixed → two side-by-side; empty → bar collapses entirely so there's no empty colored strip); (3) `handleBulkSetAvailability` filters `selectedIds` down to ids whose `available !== target` before sending — `Array.from(selectedIds)` replaced by `items.filter(...).map(i => i.id)`, so the server payload, the confirmation Alert title (`Mark N items unavailable?`), and the optimistic local update (`flippedSet` instead of `selectedIds`) all agree on the actual flip count. No callable change — `bulkUpdateMenuAvailability` already accepts an arbitrary id list. Pinned by 10 helper tests covering empty selection, all-available, all-unavailable, mixed, phantom ids, defensive non-strict-true `available` field (undefined / null / 0), empty items list, order-independence, finite-number assertion. Suite at **1192 / 1192** (was 1182). Pure client OTA: `eas update --branch production --message "PR-NEXT-ENH-1 smart bulk labels"`.

### #5 — Cannot delete menu items (shopkeeper or admin)

- **Severity:** **Medium** — workaround is "mark unavailable" but that leaves clutter; real deletion needed.
- **Platform:** Both (feature gap).
- **Symptom:** No delete affordance on menu items.
- **Fix approach:** Soft-delete pattern — add `deletedAt: number | null` to MenuItem; new callable `deleteMenuItem` writes the timestamp; listMyShopMenu / listShopMenuPublic exclude items where `deletedAt != null`; order history (which embeds the menu snapshot at order time, not a live reference) is unaffected.
- **Status:** ✅ **SHIPPED in PR-NEXT-4** (May 31 2026). Root cause re-diagnosed during drafting: a Delete affordance DID exist on `ShopMenuItemEditScreen` calling `removeMenuItem`, but the callable's behavior was asymmetric — custom items hard-deleted (gone), global items soft-disabled via `available: false` (stayed in menu, just struck through). Shopkeepers reported it as "delete doesn't work" because the global-item case looked identical to "mark unavailable." Unified via the `deletedAt` pattern: new `deletedAt?: number | null` field on `MenuItem` (optional/back-compat); `removeMenuItem` writes `deletedAt: serverTimestamp() + available: false` for both kinds; all four listing surfaces (`listMyShopMenu`, `listShopMenuPublic`, `searchMenuPublic`, `bulkUpdateMenuAvailability`) drop `deletedAt != null` rows in-memory (Firestore `where(field, '==', null)` doesn't match absent fields → would silently exclude every legacy pre-PR menu item). Pure helper `excludeDeleted` in `src/utils/menuListingHelpers.ts` pinned by 15 tests. Customer-facing "Remove this item from your menu?" copy in `ShopMenuItemEditScreen` is now identical for custom and global. Order history unaffected — `CartItem` snapshots name/price/imageUrl at order-time, no live read of the menu doc. Client wrapper return shape narrowed from `{ deleted, softDisabled? }` → `{ ok: true }` (only known caller never read the discriminator). **Bulk delete (third action on the smart-label bar from PR-NEXT-ENH-1) → ✅ SHIPPED in PR-NEXT-ENH-2** (June 1 2026): Sudhir's testing follow-up — *"I still don't see option to do bulk delete for items. When I select an item, it give mark available or mark unavailable only."* New callable `bulkRemoveMenuItems` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:2342`) mirrors `bulkUpdateMenuAvailability` exactly, only the write payload differs — `deletedAt: serverTimestamp() + available: false + updatedAt: serverTimestamp()`, identical to the per-item `removeMenuItem` so the same `deletedAt != null` filter on every listing surface (`listMyShopMenu`, `listShopMenuPublic`, `searchMenuPublic`, `bulkUpdateMenuAvailability`) drops these docs identically. Already-deleted items are skipped (`deletedCount` reflects the actual writes; `skippedCount = requestedCount - deletedCount` covers ids that don't belong to the shop OR are already deleted). Same 100-id cap, same shopOwner-strict-equality + shopId claim posture as the parent callable; pure validator `validateBulkRemoveRequest` in `@c:\Users\dahiy\grocery-mvp\functions\src\bulkRemoveMenuHelpers.ts` mirrors `validateBulkMenuRequest` minus the `available` field. Best-effort audit log via `writeAuditLog({ actorRole: 'shopOwner', actionType: 'shop.bulk_menu_remove', metadata: { requestedCount, deletedCount, skippedCount } })`. Client surface: third button on the bulk action bar in `@c:\Users\dahiy\grocery-mvp\src\screens\shop\ShopMenuScreen.tsx:684-739`. Bar restructured into two stacked rows — Row 1 (ENH-1 smart Mark buttons, side-by-side flex:1) + Row 2 (the new full-width destructive Delete button). Outer guard widened from `(bulkAvailableCount > 0 || bulkUnavailableCount > 0)` to `selectedIds.size > 0` so the Delete button is the always-visible action whenever there's any selection. Confirmation Alert subtitle reassures shopkeepers that *"Past orders that included these items are unaffected (the order keeps a snapshot of name + price + image)."* New `destructive` variant added to the shared `Button` component (`@c:\Users\dahiy\grocery-mvp\src\components\common\Button.tsx`) — red surface (`colors.danger`) + white text + same activity-indicator-on-loading semantics as `primary`. Pinned by 14 helper tests in `@c:\Users\dahiy\grocery-mvp\tests\functions\bulkRemoveMenuHelpers.test.ts` covering the full claim matrix + boundary cases. Suite at **1206 / 1206** (was 1192). Server-first deploy: `firebase deploy --only "functions:bulkRemoveMenuItems"` → verify `allUsers` IAM on the new `bulkremovemenuitems` Cloud Run service → `eas update --branch production --message "PR-NEXT-ENH-2 bulk delete"`.

### #6 — No in-shop search; need search history too

- **Severity:** **Medium** today (small menus), **High** at scale (1000-item shops).
- **Platform:** Both.
- **Symptom:** Customer enters a shop's menu screen → only scroll, no search. Same for shopkeeper menu management and admin shop view.
- **Fix approach:** Add in-shop search bar to ShopDetailScreen (customer side), ShopMenuScreen (shopkeeper side), and the admin equivalent. Filter the existing menu list client-side (fast, no callable needed) for sub-1000-item lists; for larger, consider server-side. Plus: persist last 5 search queries per user role in AsyncStorage and surface them as quick-pick chips when the search bar focuses.
- **Status:** ✅ **SHIPPED in PR-NEXT-9** (May 31 2026). Two surfaces wired (customer `ShopDetailScreen`, shopkeeper `ShopMenuScreen`); admin surface deferred since `ShopDetailManagementScreen` doesn't render a menu list today (it's metadata + KYC only — no menu list to search). Pure client OTA: name-only case-insensitive substring filter in a new pure helper `@c:\Users\dahiy\grocery-mvp\src\utils\menuSearchHelpers.ts` (`normalizeSearchQuery` + `filterMenuByQuery` + `pushToSearchHistory`); thin AsyncStorage wrapper `@c:\Users\dahiy\grocery-mvp\src\services\menuSearchHistory.ts` keyed by `search-history:menu:{role}:{shopId}` so customer + shopkeeper histories at the same shop stay independent. Reusable `@c:\Users\dahiy\grocery-mvp\src\components\menu\MenuSearchBar.tsx` is uncontrolled — parent owns the value — and surfaces a focus-only chip row that collapses the moment the user starts typing (`keyboardShouldPersistTaps="handled"` so chip taps land cleanly). Customer side filters menu BEFORE the category grouping so empty categories disappear; shopkeeper side composes search BEFORE the status-filter (`available/unavailable/custom`) so chip counts reflect what's visible. Per-`(role, shopId)` history caps at 5 with dedup-then-move-to-front semantics; history writes fire on blur OR `onSubmitEditing` (first wins) AND on chip re-tap. Query-driven empty state renders an inline "No items match …" block distinct from the genuine no-menu-yet copy. Devanagari names work (no lower-case fold needed). Pinned by 20 new tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\menuSearchHelpers.test.ts` covering whitespace collapse, reference-equality returns on no-op, mixed-script normalisation, defensive non-string-name drops, custom-max truncation, and pre-normalised dedup. Suite at **1151 / 1151** (was 1131). Deferred: admin-side menu search (no surface), server-side `searchShopMenu` callable (client filter is sub-ms at pilot scale), pack-label / description search, fuzzy/typo-tolerant matching, sticky bar on scroll. **Category quick-pick chips on customer ShopDetailScreen → ✅ SHIPPED in PR-NEXT-ENH-3** (June 1 2026): Sudhir's testing follow-up — *"category level search will give more choices to the customer. sometime they don't know what to search so category search will help them."* Pure helper `filterMenuByCategory` (`@c:\Users\dahiy\grocery-mvp\src\utils\filterMenuByCategory.ts`) returns the input array by reference when `selectedCategory == null` (matches `filterMenuByQuery`'s posture so the screen's useMemo doesn't churn). `ShopDetailScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\ShopDetailScreen.tsx:77-83,152-174,311-362`) now: (1) holds a single-select `CategoryId | null` state above the existing useMemos; (2) composes a new `categoryFilteredMenu` useMemo BETWEEN the search filter and the section grouping (search applies first, then category — the chip's effective result set reflects post-search items); (3) renders a horizontal `ScrollView` chip row directly below the `MenuSearchBar` with `keyboardShouldPersistTaps="handled"` so chip taps land while the search input is focused; (4) widens `ListEmptyComponent` to four branches (search+category, search-only, category-only, neither) using a small `labelForCategory` helper for human-readable copy. Single-select semantics: tapping the same chip clears, tapping a different chip swaps. Active chip uses `colors.primaryLight` + `colors.primaryDark` text on a pill border. Pinned by 6 helper tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\filterMenuByCategory.test.ts` covering reference-equality on null, matching / non-matching / mixed selections, order preservation, malformed `category` defense, and empty-items list. Suite at **1212 / 1212** (was 1206). Pure client OTA: `eas update --branch production --message "PR-NEXT-ENH-3 category chips on ShopDetail"`. Deferred: multi-select chips, item counts per chip, shopkeeper-side mirror, AsyncStorage persistence of selected category, hierarchical sub-categories.

### #7 — Delivery dashboard: frequent "Network connection lost" errors

- **Severity:** **High** — disrupts the partner's primary workflow.
- **Platform:** iOS at least (screenshot attached); probably Android too.
- **Symptom:** Delivery dashboard repeatedly shows "The network connection was lost. Retry." over a few minutes. Network is otherwise fine.
- **Root cause:** TBD. Watchers (watchAvailableDeliveries, watchMyDeliveries) poll at 10/15s intervals; transient HTTP failures may be surfacing as a banner instead of being silently retried. Or it's an RNFB callable issue specific to delivery callables.
- **Fix approach:** Diagnose first — add a Sentry breadcrumb on each watcher fetch error with HTTP code + message. Then either: (a) silence transient errors and only show the banner after N consecutive failures, OR (b) fix the underlying network issue if it's reproducible. Likely (a) — defensive UX.
- **Status:** ✅ **SHIPPED in PR-NEXT-5** (May 31 2026). Diagnosis: the existing reconciler in `DeliveryDashboardScreen` already required BOTH watchers (`watchAvailableDeliveries` + `watchMyDeliveries`) to be in error state before showing the banner — but had no temporal defense. A single shared blip (Cloud Run cold start, iOS TCP idle reap, brief Wi-Fi/cellular hand-off) put both watchers in error on the same tick → banner showed instantly → next 10–15s poll succeeded → banner cleared → another blip brought it back. Fixed with a per-watcher consecutive-failure counter via the new pure helper `applyPollOutcome` in `src/utils/pollFailureGate.ts` (17 unit tests pinning the matrix). Banner now only shows once BOTH watchers reach `POLL_FAILURE_THRESHOLD = 3` consecutive failures (~45s of real outage at the slower 15s cadence). Single success on either watcher resets that watcher's counter. Sentry signal: `captureMessage` fires once per outage event (gated by the helper's `justTripped` flag), with breadcrumbs on every failed poll capturing the `consecutiveFailures` count + `functionsCode` for diagnostic context.

### #8 — Online toggle doesn't persist across screen navigations

- **Severity:** **High** — partner thinks they're online (and so do they expect to receive pickups), but the system thinks they're offline → no pickup pushes → no work.
- **Platform:** Both.
- **Symptom:** Delivery dashboard → toggle Online → navigate away → return to dashboard → toggle shows Offline again.
- **Root cause:** The dashboard's `online` local state initializes to `false` on every mount and never reads from `users/{uid}.deliveryStatus`. The toggle writes the server state correctly (PR confirmed in `setDeliveryStatus`) but the UI doesn't reflect the persisted value on next mount.
- **Fix approach:** On dashboard mount, fetch current `users/{uid}.deliveryStatus` (via a new lightweight callable `getMyDeliveryStatus` or by including it in an existing profile read) and initialize the toggle from that value.
- **Status:** ✅ **SHIPPED in PR 50** (May 31 2026). The dashboard now calls the new `getMyDeliverySettings` callable inside its existing `useFocusEffect` and re-hydrates both the Online switch and the notification-radius input from authoritative server state on every focus. Verified by smoke step #2 of the PR 50 acceptance checklist.

### #9 — Show shopkeeper the count of online delivery partners in their service area

- **Severity:** **Enhancement** (not pilot-blocker, but builds shopkeeper trust).
- **Platform:** Both.
- **Symptom:** Shopkeeper has no visibility into whether anyone will actually pick up the order they're preparing.
- **Fix approach:** Lightweight callable that counts `users` where `isDelivery && deliveryStatus === 'online'` and (optionally) within haversine of the shop's location. Display on ShopOwnerDashboard as a small badge ("3 partners online nearby").
- **Status:** ✅ **SHIPPED in PR-NEXT-7** (May 31 2026). New shop-owner-only callable `getOnlinePartnersNearMyShop` (auth: `claims.shopOwner` + `claims.shopId`; admins do NOT get this surface — they have `getOnlineDeliveryCount` on AdminOrdersScreen) that reuses **PR 50's `filterPartnersByNotificationRadius` verbatim** so the count cannot disagree with `sendNewPickupPushToDelivery`'s push fanout. Wired into a new `useOnlinePartnersNearMyShop` hook (mirror of `useOnlineDeliveryCount`'s 30s polling + 3-strike stale-clear discipline) and a chip on `ShopOwnerDashboardScreen` directly under the Today KPIs. Copy: `Checking partner availability…` (loading / permanent-fail) / `No delivery partners online nearby` / `N delivery partner(s) online nearby`. Fail-open posture: when the shop has no `location`, the helper returns `filtered: false` + the unfiltered total online count (matching the push fanout's behavior for legacy shops), and the chip surfaces a hint nudging the owner to set a location for an accurate count. Privacy: callable returns `{count, filtered}` only — no partner UIDs / names / FCM tokens / locations leak. Pure helper `computeNearbyOnlinePartnerCount` in `functions/src/nearbyPartnersCountHelpers.ts` pinned by 14 tests; hook state machine `nextNearbyPartnersState` pinned by 9 tests.

### #10 — "Picked up" status not propagating; conflicting labels on customer view

- **Severity:** **Pilot-blocker** — customer sees contradictory status, breaks trust.
- **Platform:** Both.
- **Symptom (two parts):**
  1. Out of 3 test orders, only 1 correctly showed "Picked up" propagation to customer + shopkeeper. The other 2 kept showing "Ready for pickup" even after partner tapped "I've picked it up."
  2. On the 1 that did propagate, customer sees BOTH "Out for delivery" (top) AND "Pickup ready 5 minutes ago" (bottom) simultaneously.
- **Root cause confirmed in code** (functions/src/index.ts ~3277-3286, markPickedUp):
  - `markPickedUp` writes `pickedUpAt: serverTimestamp()` but **never updates `order.status`**. The top-level status field stays as `'ready_for_pickup'`.
  - The `statusHistory` arrayUnion entry also incorrectly labels the new state as `'ready_for_pickup'` instead of (e.g.) `'picked_up'`.
  - This is a *deliberate* design (no `'picked_up'` value in the OrderStatus enum) where the picked-up state is signaled by `pickedUpAt !== null`. But the client UI inconsistently checks either `order.status` OR `pickedUpAt`, producing contradictory labels.
  - The intermittent (1-of-3) propagation may be a watcher refresh race — the customer's watcher takes a poll cycle to see the new `pickedUpAt` value, and if the top-label code path doesn't refresh on `pickedUpAt` change, the user sees stale text.
- **Fix approach:**
  1. Extract a pure helper `displayOrderStatus(order): { label, sublabel }` that reads `(status, pickedUpAt, deliveredAt, cancelledAt)` together and returns ONE coherent label. Use it everywhere status is rendered (customer, shopkeeper, delivery, admin) so the two text surfaces can't disagree.
  2. Fix `markPickedUp`'s statusHistory entry: the new state label should be `'picked_up'` (or a documented constant) — not `'ready_for_pickup'`.
  3. (Optional, larger) Add `'picked_up'` to the OrderStatus enum as an explicit intermediate state and make `markPickedUp` update `order.status` to it. Bigger change — defer until the displayOrderStatus helper stabilizes the UI.
  4. Unit-test `displayOrderStatus` exhaustively (every state combination).
- **Status:** ✅ **SHIPPED in PR-NEXT-1** (May 31 2026). `src/utils/orderStatusDisplay.ts` is the new single source of truth (synthetic `picked_up` state for `status==='ready_for_pickup' && pickedUpAt!=null`); `OrderStatusChip` + `orderEtaDisplay` consume it; `markPickedUp` now (a) writes the correct `'picked_up'` statusHistory label and (b) emits an explicit customer push (`type: 'order_picked_up'`) since `markPickedUp` doesn't change top-level `status` and the existing trigger watches `status` diffs only. 36 new helper tests pin the matrix.

### #11 — "Delivered" status not propagating to shopkeeper; no push to shop/admin

- **Severity:** **Pilot-blocker** — shopkeeper doesn't know order is done; can't update their books.
- **Platform:** Both.
- **Symptom:** Delivery partner taps "Delivered" → customer's view still shows "Out for delivery" (related to #10 UI inconsistency) → shopkeeper's view still shows "Ready for pickup" → no push to shopkeeper.
- **Root cause:** `markDelivered` (functions/src/index.ts ~3324) DOES update `order.status` to `'delivered'` and fire the existing `sendOrderStatusPush` trigger to the customer. But:
  - The trigger pushes only to the customer ("Order delivered. Enjoy!") — no push to shopkeeper or admin.
  - Shopkeeper's dashboard list query may exclude `'delivered'` orders, so the delivered order disappears from the active-orders view without explicitly signaling "this completed." Worse: if the dashboard caches the prior status, the shop sees stale "Ready for pickup" until manual refresh.
  - The customer-side `'Out for delivery'` ghost label is the same as #10 — fixed by the displayOrderStatus helper.
- **Fix approach:**
  1. Extend `sendOrderStatusPush` to fan out to shopkeeper + admin on `delivered` transition (and on `cancelled`, see #2).
  2. Audit shop dashboard query to ensure delivered orders appear in a "Delivered today" section (or at least disappear cleanly from active without the stale label).
- **Status:** ✅ **SHIPPED in PR-NEXT-1** (May 31 2026, partial). The push fan-out half landed: `markDelivered` now emits explicit `pushToOwner` + `pushToAdmins` calls (in addition to the customer push from the existing `sendOrderStatusPush` trigger). The shopkeeper / customer / admin order-detail labels also resolve through `displayOrderStatus` so the stale "Ready for pickup" / "Out for delivery" ghost labels are gone. The shop-dashboard "Delivered today" section enhancement (#16(d)) stays deferred to a later PR — not pilot-blocking once the push fires.

### #12 — COD payment: customer conversion + delivery-partner confirmation

- **Severity:** **Pilot-blocker for COD-only customers** — two related gaps in the COD flow; together they account for the entire payment-trust story for any order placed as COD.
- **Platform:** Both.
- **Scope (two parts, ship together as PR-NEXT-3):**
  - **Part A — Customer-initiated COD → online conversion (Sudhir, May 31).** Customer placed a COD order but wants to pay online any time before delivery (forgot cash, prefers UPI now, etc.). Mirrors Swiggy/Zomato. Add a "Pay online now" button on Customer OrderDetailScreen, gated on `paymentMethod === 'cod'` AND `paymentStatus !== 'paid'` AND status not in `{delivered, cancelled}`. Server mints a fresh Razorpay session → existing `confirmPayment` flips paymentStatus to `paid`.
  - **Part B — Delivery-partner COD confirmation (original finding).** When the partner arrives with a still-COD order (customer didn't convert), the "Delivered" CTA is gated behind a mandatory "Mark as paid (Cash) / Mark as paid (UPI partner-collected)" step. Server stamps `paymentStatus: 'paid'` + `paidMethod` + `paidAt`. Online-prepaid orders skip this step entirely.
- **Fix approach:**
  - New callable `payCodOrder({orderId})` for Part A (separate from `retryPayment`, which has different "online attempt failed" semantics). Transaction-guarded refusal if already paid (race with partner's Part B path).
  - **Design decisions locked by Sudhir (May 31):**
    - **`paymentMethod` stays `'cod'`** on conversion. New field `paidMethod: 'cash' | 'online'` captures actual settlement. Preserves the original-intent signal for analytics ("how often do COD customers convert?").
    - **No reverse path** (online → COD not supported).
    - **Fan-out push on COD → online conversion** to shop owner + admin + delivery partner (if `deliveryPersonId` already set). Either piggyback on the existing `sendOrderStatusPush` trigger (extend to fire on `paymentStatus` 'pending' → 'paid' AND `paymentMethod === 'cod'`), or fire directly from inside `confirmPayment` when the order was originally COD. Direct-fire is cleaner — no risk of double-firing for new online-from-the-start orders. Audience-mapping helper to be exhaustively unit-tested same as PR-NEXT-1's pattern.
  - Tests pin: the convert-while-already-paid race, the partner-flow skip when already paid, the status-gate on the conversion button, and the fan-out audience on `cod → online` conversion (specifically: does NOT push to anyone on regular online order payment).
- **Status:** ✅ **SHIPPED in PR-NEXT-3** (May 31 2026). Both parts landed:
  - **Part A** — new `payCodOrder` callable + `Pay online now` button on `OrderDetailScreen` mints a fresh Razorpay session for a COD order; on success the existing `confirmPayment` callable stamps `paidMethod: 'online'` (new field) atomically with `paymentStatus: 'paid'` and fans out an `order_cod_converted` push to shop owner + admin + assigned delivery partner. `paymentMethod` stays `'cod'` as a locked-design analytics signal.
  - **Part B** — new `confirmCodPayment` callable + Cash/UPI selector pills on `ActiveDeliveryCard` (`DeliveryDashboardScreen`). The "Delivered" CTA is now gated by `markDelivered`'s new `validateMarkDeliveredCodGate` precondition for COD-unpaid orders; the partner must call `confirmCodPayment({orderId, paidMethod})` first, which stamps `paymentStatus: 'paid'` + `paidMethod` + `paidAt`. Server returns `{alreadyPaid: true}` on the Part A race-guard so the partner UI shows a "Customer paid online" toast and falls through to Delivered on the next watcher tick.
  - Deep-link: `AuthBootstrap` routes the new `order_cod_converted` push type with the same audience precedence as `order_delivered` / `order_cancelled`.
  - Tests: 37 new cases in `tests/functions/codPaymentHelpers.test.ts` pin the precondition matrix for all three callables + the fan-out decision. Suite at **1016 / 1016** (was 979).

### #13 — Delivery proof photo

- **Severity:** **Enhancement** but high value for dispute resolution.
- **Platform:** Both (camera capture).
- **Symptom:** No photographic record of delivery; "items missing / never received" disputes have no evidence.
- **Fix approach:** Optional photo capture step in the delivery-completion flow. Upload to Firebase Storage at `delivery-proofs/{orderId}.jpg`. Shopkeeper + admin can view from order detail. Make it optional initially; can make required later if disputes warrant.
- **Status:** ✅ **SHIPPED in PR-NEXT-6** (May 31 2026). Three new callables (`getDeliveryProofUploadUrl`, `recordDeliveryProofUpload`, `getDeliveryProofReadUrl`) implement a v4-signed-PUT → record → v4-signed-READ pipeline mirroring PR 6.1 (`/menu/`) and PR 31 (`/shop-kyc/`). Storage path is deterministic `delivery-proofs/{orderId}.jpg` (one photo per order; re-upload overwrites). Auth gates: upload + record require `claims.delivery === true` AND assignee match AND `pickedUpAt > 0`; reads role-mixed (customer of order / shop owner of shop / admin / assigned partner — each independently checked). Photo is OPTIONAL by design — `markDelivered` does NOT require it (door-handoff / camera-permission-denied cases). Storage rules add explicit `/delivery-proofs/` deny-all alongside `/menu/` and `/shop-kyc/`. Two schema-additive `Order` fields (`deliveryProofStoragePath`, `deliveryProofUploadedAt`). Client: `pickAndResizeImage('camera')` (already wired for menu + KYC — no permission/native changes) → `uploadDeliveryProof` orchestrator → optional photo button on `ActiveDeliveryCard` (lower visual weight, parallel to Delivered CTA, never blocks delivery). Reusable `DeliveryProofViewer` component renders thumbnail + tap-to-zoom modal on shop / customer order detail. Pinned by 19 helper tests + 5 upload-orchestration smoke tests + 7 `formatPaymentMethod` tests; suite at **1131 / 1131** (was 1089). **Photo upload Timestamp-vs-number bug → ✅ SHIPPED in PR-NEXT-HOTFIX-1** (May 31 2026): during testing, every upload attempt failed with `failed-precondition: "Pick up the order before…"` even on demonstrably picked-up orders. Root cause: `markPickedUp` writes `pickedUpAt: FieldValue.serverTimestamp()`, which the Admin SDK reads back as a `Timestamp` object (not millis) — but `validateDeliveryProofUploadAuth` gated on `typeof order.pickedUpAt !== 'number'`, so the strict-typeof check rejected every real production read. The test fixture used a millis number and masked the bug. Fix widens the validator to accept both shapes (plain millis number OR Timestamp-like with `.toMillis()` method) and normalises via `.toMillis()` before the `> 0` + `Number.isFinite` gate; null/undefined/wrong-shape/NaN/Infinity/0 still reject. 4 new test cases cover the production Timestamp shape, zero-millis Timestamp, NaN-millis Timestamp, and non-Timestamp object. Suite at **1155 / 1155** (was 1151). Server-only deploy (`firebase deploy --only "functions:getDeliveryProofUploadUrl,functions:recordDeliveryProofUpload"`); no client OTA. **Defensive sweep finding (filed for follow-up, not fixed in HOTFIX-1):** `customerCancelWindowHelpers.ts:136` has the SAME bug pattern on `paidAt` — `cancelMyRecentPaidOrder` passes raw `orderSnap.data()` into `canCustomerCancelPaidOrder`, which gates `typeof order.paidAt !== 'number'`; production `paidAt` is written via `FieldValue.serverTimestamp()` (`index.ts:1383, 3930`), so the cancel-paid-order flow may currently mis-gate. `index.ts:7055-7056` (`getMyProfile`) silently coerces `out.createdAt`/`out.updatedAt` Timestamp objects to `null` instead of millis (data-quality, not gate). Other matches are safe: `orderStatusTransitionHelpers.ts:90` validates a CLIENT-supplied `readyByEstimate` (not a server timestamp); `index.ts:3293` already uses `data.createdAt?.toMillis?.() ??` fallback; `index.ts:4785, 4817` are diagnostic logs only; `customerCrmHelpers.ts:80` is called after the caller pre-normalises via `.toMillis?.()`. **Partner-accept customer push + identity surface → ✅ SHIPPED in PR-NEXT-13a** (June 1 2026): Sudhir's smoke testing surfaced a gap — when the delivery partner accepted a pickup, the customer got no notification and the partner's identity wasn't visible until the actual pickup event. The gap can be 5–30 minutes of customer-side opacity ("Out for delivery" push had fired on the status flip to `ready_for_pickup`, but no partner had necessarily claimed yet). Fix extends `claimDelivery` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:3480`) to do two post-transaction best-effort steps: (1) read the partner's `users/{uid}.displayName` via the new pure helper `pickPartnerDisplayName` (`@c:\Users\dahiy\grocery-mvp\functions\src\claimDeliveryHelpers.ts`) and denormalise it onto the order as `deliveryPersonName`, and (2) fire `pushToUser(customerUid, …, type: 'order_partner_accepted')` with body `"<partnerName> will pick up your order from <shopName>."` Both steps wrapped in `try/catch` so a name-lookup or push failure cannot roll back the successful atomic claim. New schema-additive `Order.deliveryPersonName?: string` field (legacy + mid-flight orders absent → fallback copy). Client adds the new `PartnerIdentityCard` component (`@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerIdentityCard.tsx`) which renders an initials-in-coloured-circle avatar + display name + state-aware subtitle (📦 Heading to the shop / 🛵 On the way to you) on `OrderDetailScreen` whenever `deliveryPersonId` is set. Phone number stays gated to post-pickup (no change to existing privacy posture). Real partner profile photos deferred — KYC selfie is PII; initials are the v1 stand-in. Push routing added to `AuthBootstrap.tsx` as a customer-only single-target case (same posture as `order_picked_up`). Pure helper `initialsFor` (`@c:\Users\dahiy\grocery-mvp\src\utils\partnerInitials.ts`) lives in its own pure `.ts` file so the test suite can pin the avatar-glyph logic without dragging the `.tsx` component through the JSX-free `tests/tsconfig.json`. Pinned by 8 `pickPartnerDisplayName` tests + 9 `initialsFor` tests; suite at **1172 / 1172** (was 1155). Deploy: server-first `firebase deploy --only "functions:claimDelivery"` + IAM verification on the `claimdelivery` Cloud Run service + client OTA. **`paidAt` Timestamp-vs-number bug in `customerCancelWindowHelpers` → ✅ SHIPPED in PR-NEXT-HOTFIX-2** (June 1 2026): the same Timestamp-vs-number bug class that bit `pickedUpAt` in HOTFIX-1 was sitting dormant in `canCustomerCancelPaidOrder` (`@c:\Users\dahiy\grocery-mvp\functions\src\customerCancelWindowHelpers.ts:135-137`). The validator gated on `typeof order.paidAt !== 'number'`, but the production webhook (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:1383` + `:3930`) writes `paidAt: FieldValue.serverTimestamp()` — Admin SDK reads back a `Timestamp` object, not millis. Every customer attempting to cancel a paid online order in their 2-minute self-service window would have been rejected with `"Order has no paid timestamp"`, with the in-app countdown UI on `OrderDetailScreen` still saying "1:42 remaining" because RNFB's client-side serializer flattens Timestamps for the customer device. Maximally confusing UX, fully blocking the self-service refund path. Bug stayed latent because Razorpay was suspended during the May 30 pilot — no online prepaid orders were being created in production. **The moment Razorpay restores, this would have activated as a pilot-blocker.** §C defensive sweep from HOTFIX-1 had already flagged this exact line; HOTFIX-2 is the queued fix landing now. Same `toMillis()`-narrowing pattern HOTFIX-1 applied to `validateDeliveryProofUploadAuth`: accept plain millis (test fixtures + pre-normalized callers) OR Timestamp-likes (real Firestore reads); reject `null` / non-finite / `<= 0` (Unix epoch). 6 new test cases added to `tests/functions/customerCancelWindowHelpers.test.ts` (Timestamp-like happy path, epoch 0, NaN, no-`toMillis` object, Timestamp-like + window boundary, Timestamp-like past window) — composes the new shape with every existing window-boundary semantic. Suite at **1182 / 1182** (was 1176). Twice in two days against the same bug class promoted to `.windsurf/code-discipline.md` Rule 12. Server-only deploy: `firebase deploy --only "functions:cancelMyRecentPaidOrder"` (already has `allUsers` binding — no IAM check needed). **Post-delivered photo CTA → ✅ SHIPPED in PR-NEXT-13d** (June 1 2026): Sudhir's HOTFIX-1 smoke testing surfaced a follow-up gap — if the partner forgot to capture the proof photo before tapping Delivered, the CTA disappeared with the active card and there was no recovery path. Server side was already correct: `validateDeliveryProofUploadAuth` (post-HOTFIX-1) gates only on `delivery` claim + `deliveryPersonId === auth.uid` + `pickedUpAt != null` — there is no `deliveredAt` gate, so a partner can upload a missed proof any time while still the assignee. Pure client surface fix: `DeliveryHistoryCard` (`@c:\Users\dahiy\grocery-mvp\src\screens\delivery\DeliveryDashboardScreen.tsx:1200`) now takes the same `onAddPhoto` / `uploadingPhoto` / `hasProof` props as `ActiveDeliveryCard` and renders the same `📸 Add delivery proof (optional)` / `📸 Photo added — re-take?` button at the bottom of the card, threaded from the same parent-level `handleAddDeliveryProof` handler + `photoUploading` + `recentlyUploadedProof` state PR-NEXT-6 already wired. Tap-target isolation via inner `Pressable`'s `e.stopPropagation()` so tapping the photo button doesn't bubble to the card-level `onPress` that navigates to `DeliveryOrderDetail`. Reuses `photoBtn` / `photoBtnText` / `photoBtnDisabled` styles verbatim. No new helpers, no new tests (PR-NEXT-6's existing `uploadDeliveryProof` + `deliveryProofHelpers` surface area covers the contract; this is presentation reuse). Upload window is intentionally **unbounded for v1** — partner can re-upload any time while still assigned to the order. Revisit if pilot disputes show late-upload abuse. Suite at **1172 / 1172**. Pure OTA: `eas update --branch production --message "PR-NEXT-13d photo CTA on delivered"`.

### #14 — Reorder: unavailable item's X button doesn't work

- **Severity:** **High** — blocks reorder flow completely if any item is unavailable.
- **Platform:** Both.
- **Symptom:** Reorder flow shows previous order; if an item is "no longer offered by the shop," it's listed with a red X to remove. Tapping the X does nothing.
- **Root cause:** TBD — likely the onPress handler on the X icon isn't wired, or the handler doesn't update the local state used to render the line items.
- **Fix approach:** Trace the X press handler in the reorder screen (likely `RepeatOrderScreen` or similar; PR 13 from earlier). Wire to a local state mutation that filters the item out of the planned cart.
- **Status:** ✅ **SHIPPED in PR-NEXT-8** (May 31 2026). Smoking gun confirmed in `src/components/order/ReorderModal.tsx:255` — the ✕ was a static `<Text>`, not a `Pressable`, no `onPress` handler. The underlying availability filtering was already correct (`planToCartItems` already drops anything that isn't `available_*`); the bug was purely that the ✕ had no meaning that matched what users expected when they tapped it. Fixed by wiring the ✕ to a real `Pressable` with `hitSlop={12}` + `accessibilityLabel`; tap adds the row's `menuItemId` to a modal-local `Set<string>`, render path filters dismissed IDs out of the visible Unavailable list, section title decrements then disappears when all rows are dismissed. CTA copy + cart contents are unaffected (dismissal is presentation-only). State is ephemeral — closing/reopening the modal restores the full Unavailable list (effect resets on `buildPlanKey(plan)` change). Pure helper `addDismissedId` + `buildPlanKey` in `src/utils/reorderModalDismissals.ts` pinned by 14 tests (covers immutable update, idempotent re-dismissal, null/undefined/empty-string no-op, plan-key stability, line-order sensitivity).

### #15 — "Order Again" home card: confusing label vs content

- **Severity:** **Medium** — UX confusion.
- **Platform:** Both.
- **Symptom:** Home shows "Order Again" card with "3 orders" subtitle. Tap → shows items (not orders), with Cancel / "Add 3 items to cart" buttons. The "3 orders" label suggests a list of past orders, but it's actually showing a single bundle of items.
- **Fix approach:** Either: (a) change the subtitle to "X items from your most recent order" so it accurately describes the next screen, OR (b) make the destination actually show a list of past orders the customer can pick from.
- **Status:** ✅ **SHIPPED in PR-NEXT-8** (May 31 2026). Picked option (a) — change the subtext to be action-predictive ("Last order · {N} items") and drop the lifetime count from the card. Lifetime frequency is still implicit in the rail ordering itself (most-frequent shop comes first; PR 14's sort is unchanged), so no information is lost. Required a new optional `lastOrderItemCount: number` field on `FrequentShopEntry`, populated client-side from `mostRecent.items.length` inside the existing pure-helper loop; defensive `Array.isArray` guard so a malformed order doc renders "Last order · 0 items" rather than crashing the rail. Schema-additive only (no callable / Firestore changes). 4 new pinned tests in `tests/utils/pickFrequentlyOrderedShops.test.ts` covering most-recent-not-lifetime semantics, missing/non-array `items` field, and empty-array baseline.

### #16 — Notify shopkeeper + customer + admin on Delivered; surface payment + photo evidence

- **Severity:** **High** (composite enhancement — partial overlap with #11, #12, #13).
- **Platform:** Both.
- **Symptom:** Delivery completion is opaque to shop and admin. No verification of payment received or delivery executed.
- **Fix approach:** Composite: (a) push fan-out to shop + admin on Delivered (overlap with #11), (b) payment confirmation step (#12), (c) photo capture (#13), (d) admin/shopkeeper order-detail view shows the payment method + photo for verification.
- **Status:** ✅ **FULLY SHIPPED.** Sub-(a) shop + admin push on Delivered ✅ **SHIPPED in PR-NEXT-1** (May 31 2026) via `markDelivered`'s explicit `pushToOwner` + `pushToAdmins` calls. Sub-(b) COD payment confirmation ✅ **SHIPPED in PR-NEXT-3** (May 31 2026) — `confirmCodPayment` callable + Cash/UPI pills on the delivery dashboard gate `markDelivered` for COD-unpaid orders. Sub-(c) photo capture ✅ **SHIPPED in PR-NEXT-6** (May 31 2026) via the three signed-URL callables + `DeliveryProofViewer` component (see #13 for the full description). Sub-(d) order-detail payment + photo evidence ✅ **SHIPPED in PR-NEXT-6** (May 31 2026) — a new `formatPaymentMethod` pure helper renders the actual settlement method (`Cash on delivery — paid online (converted)`, `Cash on delivery — paid in cash`, `Online (paid up front)`, etc.) on both `ShopOrderDetailScreen` and customer `OrderDetailScreen`, replacing the old display that mislabelled COD-converted orders as `Cash on Delivery` even when Razorpay had actually settled them. The `DeliveryProofViewer` component renders on both screens with on-mount signed-READ minting. Admin UI integration ✅ **SHIPPED in PR-NEXT-6.1** (May 31 2026) — `AdminOrdersScreen` now renders the same `formatPaymentMethod`-driven `Paid via …` line on every card AND a third per-card disclosure (`📸 Delivery proof`) alongside the existing Manual-override + Full-timeline disclosures, only shown when `deliveryProofStoragePath` is stamped (no dead trigger on proof-less orders). The three disclosures intentionally keep INDEPENDENT one-card-at-a-time state so an admin can review the photo with the timeline simultaneously — the cross-reference flow dispute resolution needs. Single-file change; reuses `DeliveryProofViewer` + `formatPaymentMethod` verbatim; no new helpers, no new tests.

### #17 — Conflicting "Pickup ready in/ago" countdown shows alongside the new ready-for-pickup status label

- **Severity:** **Medium** — UX inconsistency, not a pilot-blocker, but contradictory labels erode customer trust in real time.
- **Platform:** Both.
- **Symptom:** Customer places an order. Shop marks `ready_for_pickup`. On `OrderDetailScreen` the customer sees TWO lines simultaneously:
  1. Top status block: `Ready — Partner is picking up` (correct, post-PR-NEXT-1 unified status display).
  2. Below it: `Pickup ready in X minutes` while still in-window, then `Pickup ready X minutes ago` once past the ETA. **This second line is the pre-ready ETA countdown that should have been retired the moment the order actually became ready.** Customer has to read both, decide which to believe, and the "ago" wording makes it feel like the system is contradicting itself.
- **Root cause (likely):** PR-NEXT-1 unified the order-status display vocabulary via `src/utils/orderStatusDisplay.ts` and wired `OrderStatusChip` + `orderEtaDisplay` to consume it. But the `readyByEstimate` countdown sub-line on `OrderDetailScreen` appears to be a SEPARATE render path (probably an inline `formatRelativeReadyBy(order.readyByEstimate)` or similar) that wasn't updated when PR-NEXT-1 unified the top label. Both render independently from the same `order` doc; only the top label respects the synthetic `ready_for_pickup` semantics. **Half the job got done.**
- **Scope:** `src/screens/OrderDetailScreen.tsx` — find the inline "Pickup ready in/ago" render block (search for `readyByEstimate` or `Pickup ready` in that file).
- **Fix approach:** When `order.status === 'ready_for_pickup'`, suppress the countdown line entirely (the order IS ready — no time-based ETA copy is meaningful). Alternative: replace the countdown with a fresh-context message like `📦 Delivery partner on the way` (matches the PartnerIdentityCard subtitle from PR-NEXT-13a once that ships). Cleanest v1 is suppress; can layer the fresh-context message in a follow-up if pilot feedback wants it. Pure client OTA.
- **Status:** ✅ **SHIPPED in PR-NEXT-17** (June 1 2026). One-branch addition to `orderEtaDisplay` (`@c:\Users\dahiy\grocery-mvp\src\utils\orderEtaDisplay.ts:111`): when `order.status === 'ready_for_pickup'`, return `{ kind: 'hidden' }`. Mirrors the existing PR-NEXT-1 `pickedUpAt`-aware suppression branch (which handled the post-pickup case); this PR closes the matching gap for the post-ready / pre-pickup window. The "likely cause" hypothesis above was slightly off — the second line wasn't a separate render path but the same `orderEtaDisplay` helper's `ready_by` branch firing for `ready_for_pickup`-status orders, because step 4 of the state machine (`readyByEstimate` valid → `ready_by`) didn't filter on status. The new branch lands after the `pickedUpAt` branch and before the `pending` branch so the "in-flight progression" branches stay grouped at the top. `deliveryPersonId` deliberately NOT added to `EtaInput` — both pre-partner-claim and post-partner-claim sub-windows produce the same suppression decision, and the visual difference is already carried by PR-NEXT-13a's `PartnerIdentityCard` appearing/disappearing. Render sites verified: `OrderDetailScreen`, `OrderConfirmationScreen`, and `ActiveOrdersRail` all dispatch on `kind === '...'` conditionals — `hidden` naturally renders nothing. `ActiveOrdersRail` had its own pre-helper `ready_for_pickup` early-returns (`'Out for delivery'` / `'Almost ready'`) so its copy is unchanged. Pinned by 1 flipped test (`ready_for_pickup + future readyByEstimate` now expects `hidden`) + 4 new tests (elapsed-time `readyByEstimate`, defensive `ready_for_pickup` with no `readyByEstimate`, two regression anchors for `accepted` + `preparing` still returning `ready_by`). Suite at **1176 / 1176** (was 1172). Pure OTA: `eas update --branch production --message "PR-NEXT-17 ETA suppression on ready_for_pickup"`.
- **Workaround until fix shipped:** customer ignored the lower line; trusted the top status block.

### #18 — Cold-start push tap loses its deep-link target (lands on Home)

- **Severity:** **High** — every cold-start push tap silently misroutes, across every role. Symptom Sudhir reported is shopkeeper-specific ("after placing order, shopkeeper got notification, I clicked on notification as shopkeeper, it brought me on home page instead of that order") but the root cause is role-agnostic.
- **Platform:** Both (push-tap lifecycle plumbing in JS).
- **Symptom:** App is fully closed (force-quit). A push arrives. User taps it. App launches and lands on Home / default landing instead of the deep-link target the warm-tap path would have routed to.
- **Root cause:** `Notifications.addNotificationResponseReceivedListener` fires only for taps that happen AFTER the listener attaches. Cold-start taps (where the notification tap launches a closed app) are consumed by Expo internally before `AuthBootstrap`'s useEffect ever runs, so they're available exclusively via `Notifications.getLastNotificationResponseAsync()`. No call to that API existed anywhere in `src/` pre-fix. Bonus failure: even with the response in hand, `safeNavigate` no-ops while `navigationRef.isReady()` is false — at cold-start the NavigationContainer mounts asynchronously, so a same-tick dispatch hits the warn-and-drop branch.
- **Status:** ✅ **SHIPPED in PR-NEXT-HOTFIX-5** (June 1 2026). Single-file edit in `@c:\Users\dahiy\grocery-mvp\src\components\AuthBootstrap.tsx`: (1) extracted the 165-line inline routing table out of the `addNotificationResponseReceivedListener` callback into a named `handleNotificationResponse(response)` function — every branch / return / `safeNavigate` call preserved verbatim — so the same routing can be invoked from both the warm-tap listener AND the new cold-start dispatch; (2) added an `getLastNotificationResponseAsync().then(...)` call with a polling race-guard that fires `handleNotificationResponse` exactly once when BOTH `navigationRef.isReady()` and `useAuthStore.getState().ready` are true (100ms tick, 10s safety ceiling, `coldStartDispatched` flag for double-dispatch defense against Android OEM quirks); (3) added cleanup so a component unmount cancels any in-flight poll and marks the dispatch flag so straggling ticks self-abort. Imported `navigationRef` from `../navigation/navigationRef` (carries DO-NOT-REMOVE comment). No new tests — pure lifecycle plumbing, manual acceptance covers it. tsc + 1212 / 1212 unit tests still clean. Pure client OTA: `eas update --branch production --message "HOTFIX-5 cold-start push deep-link"`. Deferred: migration to the reactive `useLastNotificationResponse` hook, Sentry capture on 10s timeout (currently `console.warn` only).
- **Workaround until fix shipped:** customer / shopkeeper / partner / admin navigates manually to the right list screen and finds the relevant order or item by hand.

### #19 — Delivery fee inconsistency: shop list shows ₹25, checkout shows ₹100

- **Severity:** **High** — pricing-trust failure. Customer sees a low number on the shop card, commits to building a cart, then sees a bigger number at checkout and abandons.
- **Platform:** Both (pure client display gap).
- **Symptom:** Customer's shop card / shop detail meta line displays `shop.deliveryFee` (the flat legacy fallback — `₹25` on a typical pilot shop), while CheckoutScreen displays the distance-based charge from `chargeForDistance(shop.deliveryChargeTiers, distanceKm, ...)` (e.g. `₹100` at the catch-all tier 4+ km away).
- **Root cause:** PR 47 introduced distance-based tiered delivery charges via `chargeForDistance(tiers, distanceKm, fallbackFlat)` and wired CheckoutScreen + ShopSettingsScreen to it, but the customer-facing shop card (`@c:\Users\dahiy\grocery-mvp\src\components\shop\ShopCard.tsx:57`) and shop detail meta (`@c:\Users\dahiy\grocery-mvp\src\screens\ShopDetailScreen.tsx:284`) still rendered `shop.deliveryFee` directly. Two surfaces, two numbers.
- **Status:** ✅ **SHIPPED in PR-NEXT-HOTFIX-6** (June 1 2026). New pure helper `@c:\Users\dahiy\grocery-mvp\src\utils\displayDeliveryCharge.ts` wraps `chargeForDistance` and resolves the distance via a three-step preference: customer's live location (`useLocationStore`) → haversine against `shop.location` (freshest, matches what `listShopsPublic` would have stamped) → server-stamped `shop.distanceKm` → flat `shop.deliveryFee` fallback. Wired into `ShopCard.tsx` (now takes a new optional `customerLocation?: GeoPoint` prop, threaded from `ShopListScreen` via the existing `useLocationStore` consumer) and `ShopDetailScreen.tsx` (reuses its existing `location` from the store). CheckoutScreen intentionally NOT switched — it has additional concerns (PR 46 `getDeliveryEstimate` against the customer's CHOSEN delivery address, not their live GPS), so the two surfaces can disagree by design when those references differ. ShopSettingsScreen is shop-owner-side and stays on the raw editable `deliveryFee` field. Pinned by 6 helper tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\displayDeliveryCharge.test.ts` covering tiered + customer-location, tiered + only stamped distance, legacy shop fallback, no-distance fallback, boundary distance, and invalid customer coords. Suite at **1218 / 1218** (was 1212). Pure client OTA: `eas update --branch production --message "HOTFIX-6 delivery fee consistency"`. Deferred: reconciling the shop-detail vs. checkout reference points (different by design — shop browse vs specific delivery), tier-breakdown UI on the shop card, server-side recompute (already done in `listShopsPublic`).
- **Workaround until fix shipped:** customer abandoned the order at checkout once they saw the unexpectedly higher number.

### #20 — Shopkeeper + admin not notified on partner-claim and pickup

- **Severity:** **Medium** — visibility gap. Pre-PR shopkeeper had to watch their dashboard to see when a partner claimed; admin had no signal at all for those transitions.
- **Platform:** Both server-side (push fan-out gap).
- **Symptom:** When a delivery partner accepted a pickup (Case 5) only the customer got a push. When the partner marked picked-up (Case 7) only the customer got a push. Shopkeeper + admin had to actively poll to learn about either transition.
- **Status:** ✅ **SHIPPED in PR-NEXT-NOTIFY-EXTEND** (June 1 2026). Server: `claimDelivery` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:3584`) now captures `orderShopId` inside its existing transaction (alongside the pre-existing `customerUid` + `orderShopName` captures) and fires two new best-effort post-transaction blocks — `pushToOwner(ownerUid, "🛵 Pickup partner assigned", ...)` + `pushToAdmins("🛵 Pickup partner assigned", ...)` — both with new push type `'order_partner_assigned'` and `shopId` in the data payload for audience-aware deep-link routing. Mirrors `markDelivered`'s shop+admin pattern exactly. `markPickedUp` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:3770`) similarly fans out to shop owner + admin after the existing customer push, reusing the EXISTING `'order_picked_up'` type (audience expanded, not new type) — same `shopId`-in-data convention. Client: `AuthBootstrap` (`@c:\Users\dahiy\grocery-mvp\src\components\AuthBootstrap.tsx:328-363`) — extended the `order_picked_up` branch from customer-only to audience-aware (shopOwner-of-this-shop → ShopOrderDetail, admin → AdminOrders, customer → OrderDetail; same precedence as `order_cancelled` / `order_delivered`); added a new `order_partner_assigned` branch with the same audience routing minus the customer fall-through (defensive return — customer never receives this type, gets the parallel `order_partner_accepted` from PR-NEXT-13a). Functions tsc + root tsc + 1218 / 1218 unit tests still clean (no new pure helpers — fan-out logic is inside the callable wrappers and follows the same pattern as `markDelivered`). Server-first deploy: `firebase deploy --only "functions:claimDelivery,functions:markPickedUp"` → re-verify `allUsers` IAM on both Cloud Run services (recurring strip gotcha) → `eas update --branch production --message "PR-NEXT-NOTIFY-EXTEND shop+admin on claim+pickup"`. Note: customer-side picked-up push is FCM-blocked on Android (separate issue); shopkeeper + admin are on iOS in the pilot setup so APNs delivery works immediately. Deferred: delivery-partner pushes on shop accepted/preparing transitions (Case 3 partial — needs design on radius-filtered fan-out), notification preferences, notification grouping.
- **Workaround until fix shipped:** shopkeeper monitored the dashboard; admin had no signal at all for these transitions.

### #21 — Delivery partner: pre-claim earnings not visible on available-pickup card

- **Severity:** **Medium** — efficiency gap. Partner had to tap into detail to see what they'd earn before deciding whether to claim. Pre-decision opacity = inefficient claims.
- **Platform:** Both (pure client display gap).
- **Symptom:** Sudhir's Case 4 — *"As a delivery person before accepting, it will help me if we can show the total distance 'pickup from' and 'deliver to' plus calculated delivery charges. That way it will be full visibility to delivery partner before accepting the order."* Distance was already there (PR 49 `RideDistanceLine`); earnings were not.
- **Status:** ✅ **SHIPPED in PR-NEXT-PARTNER-VIS** (June 1 2026). One-line render addition in `@c:\Users\dahiy\grocery-mvp\src\screens\delivery\DeliveryDashboardScreen.tsx` to both `AvailablePickupCard` (`:1153-1163`) and `ActiveDeliveryCard` (`:1384-1391`): `💰 Earn ₹X` line below `RideDistanceLine` / `DeliveryLocationLabel`, gated on `typeof order.deliveryFee === 'number' && order.deliveryFee > 0` so legacy / malformed orders hide the line cleanly rather than displaying ₹0. Bold + `colors.primaryDark` (matching `rideLine` weight) so distance + earnings read as a paired headline. New `earningsLine` style entry adjacent to `rideLine`. Pure client OTA: `eas update --branch production --message "PR-NEXT-PARTNER-VIS pre-claim distance + earnings"`. No new tests — purely a render addition. tsc + 1218 / 1218 unit tests still clean. Deferred: cumulative shift earnings widget, per-tier breakdown, surge / peak-hours bonus.
- **Workaround until fix shipped:** partner tapped into the order detail to see `deliveryFee` before deciding whether to claim — extra round-trip for every available pickup.

### #22 — Customer's `PartnerIdentityCard` not tappable, no way to see partner details

- **Severity:** **Medium** — UX dead-end. Customer sees a card promising delivery-partner info but tapping does nothing.
- **Platform:** Both (pure client UI gap).
- **Symptom:** Sudhir's Case 6 — *"On customer side, I see a card 'your delivery partner' with a message 'heading to the shop' but not clickable and not able to show any details."*
- **Status:** ✅ **SHIPPED in PR-NEXT-PARTNER-CARD** (June 1 2026). `@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerIdentityCard.tsx` now wraps its outer container in a `Pressable` and accepts an optional `onPress` prop — when provided, renders a disclosure chevron (`›`) on the right with a `pressed` opacity treatment + a11y `button` role + descriptive label; when omitted, the card stays static (back-compat for any future caller wanting a read-only treatment). New `@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerDetailsSheet.tsx` is a slide-up `Modal` with backdrop dismiss + inner-press-swallow trick (matching `ReorderModal` / `CancelAndRefundModal` convention — no new bottom-sheet dependency added). Reuses the existing pure `initialsFor` helper so the avatar glyph stays in lockstep with the card. Discloses pre-pickup: display name, state subtitle (📦 / 🛵), shop pickup location, and a muted "Phone — Shared once the order is picked up" row. Phone reveal post-pickup intentionally NOT duplicated here — stays on the existing `OrderDetailScreen`-side surface to avoid two reveal paths going out of sync. `@c:\Users\dahiy\grocery-mvp\src\screens\OrderDetailScreen.tsx`: new `partnerSheetOpen` useState alongside the other useStates (above the existing early returns, Rule 2 discipline); card render now passes `onPress={() => setPartnerSheetOpen(true)}`; sheet mounts at the SafeAreaView root (outside the ScrollView) so the slide-up animation covers the full screen. Pure client OTA: `eas update --branch production --message "PR-NEXT-PARTNER-CARD tappable card + details sheet"`. No new tests — presentation logic, `initialsFor` already pinned by PR-NEXT-13a's tests. tsc + 1218 / 1218 unit tests still clean. Deferred: in-sheet phone disclosure post-pickup (existing surface owns it), partner rating display, partner profile photo (no flow exists yet), live ETA / distance to shop.
- **Workaround until fix shipped:** customer waited for the actual pickup push to learn anything beyond the static "Heading to the shop" subtitle.

### #23 — Delivery Detail screen: Delivered enabled before COD paid + Cash/UPI pills only on dashboard

- **Severity:** **Medium** — two distinct UX issues bundled. (a) Server's COD gate (PR-NEXT-3 §E `validateMarkDeliveredCodGate`) correctly rejects `markDelivered` on COD-unpaid orders, but the client showed an enabled Delivered button and surfaced the rejection as a generic alert — dead-tap-then-error UX. (b) Cash / UPI pills were on the dashboard `ActiveDeliveryCard` only; tapping the card opened the detail screen which had ONLY the Delivered button — partner had to bounce between surfaces to complete a COD flow.
- **Platform:** Both (pure client UX gap).
- **Symptom:** Sudhir's Case 8 — *"Delivery dashboard shows 'add delivery proof', cash received and UPI received options there. when I clicked on that card, it opened details about order and shows me delivered option enabled at the bottom. I clicked on delivered option and it gave error message about payment first. But why we are even giving delivered options at all if payment part is not done yet? Also giving few options at card and 1 option inside the card (at detail level) is also not good experience."*
- **Status:** ✅ **SHIPPED in PR-NEXT-COD-UX** (June 1 2026). New pure helper `@c:\Users\dahiy\grocery-mvp\src\utils\codDeliveryGate.ts` mirrors the server's `validateMarkDeliveredCodGate` matrix — `canShowDeliveredButton(order)` returns `true` only when `(method=online ∧ status=paid)` or `(method=cod ∧ status=paid)`; everything else (COD pending, online pending, cod failed, missing paymentStatus) returns `false`. Pinned by 7 helper tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\codDeliveryGate.test.ts` covering the full matrix + a defensive missing-paymentStatus case for legacy orders. `useDeliveryOrderDetail` hook (`@c:\Users\dahiy\grocery-mvp\src\screens\delivery\DeliveryOrderDetailScreen.useDeliveryOrderDetail.ts:205-253,265-276,362-412,420-429`) gained a `confirmCodPayment` injectable callable + a new `'confirmCod'` value in the `pendingAction` union + a `handleConfirmCodPayment(paidMethod)` action that mirrors the dashboard's `handleConfirmCodPayment` — same optimistic flip (`paymentStatus: 'paid' + paidMethod`), same `alreadyPaid` surfacing, same revert-on-failure-unless-watcher-already-paid posture (defensive against a parallel `payCodOrder` conversion landing mid-flight). `DeliveryOrderDetailScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\delivery\DeliveryOrderDetailScreen.tsx:111-128,360-429`) action area is now a three-way ladder mirroring the dashboard's `ActiveDeliveryCard`: pre-pickup → "I've picked it up", picked-up + COD-unpaid → Cash/UPI pills (gated on `!canShowDeliveredButton(order)`), picked-up + paid → "Delivered". Pill styles are duplicated under the same names (`codConfirmLabel` / `codConfirmSub` / `codPillRow` / `codPill` / `codPillDisabled` / `codPillText`) so the visual language matches across surfaces — partner reads the same affordance on either screen. Dashboard's existing `needsCodConfirmation` ladder structurally already gates Delivered, so no change there. Suite at **1225 / 1225** (was 1218). Pure client OTA: `eas update --branch production --message "PR-NEXT-COD-UX smart Delivered gating + detail pills"`. Deferred: shared layout refactor between card + detail (different needs), receipt generation at COD payment time.
- **Workaround until fix shipped:** partner saw the rejection alert, swiped back to the dashboard, tapped the appropriate pill, then re-entered the detail screen to complete delivery — extra round-trip every time.

### #24 — "Deliver to current location" silently saves a duplicate address with stale Home fields

- **Severity:** **Medium** — pollutes the customer's address book and undermines trust in saved addresses (over time the list fills with unlabelled near-duplicates).
- **Platform:** Both (pure client UX gap).
- **Symptom:** Sudhir's Case 10 — *"When customer selects deliver to my current location, it creates new address with name 'Address' and copy address details from Home Address. User needs to give this location a name instead of just saying 'Address', slowly there will be tons of addresses and user will not know which address is what. Also it should not copy other address details as default."*
- **Root cause:** `maybeSaveAddressAfterOrder` in `@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx:461` was firing after every order placed without a `selectedAddressId`. When the customer used "Deliver to my current location" the form fields still held the previously-selected Home address (`applySavedToForm` had populated them earlier in the session), so the silent auto-save persisted those stale Home fields as a new SavedAddress with NO label — rendering as the generic "Address" fallback in the saved-address picker (the `{addr.label || 'Address'}` site at `:905`). Stale-form copy + missing label, both at once.
- **Status:** ✅ **SHIPPED in PR-NEXT-ADDRESS-UX** (June 1 2026). Two-line behavioral fix in `@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx:486-490`: (1) skip auto-save entirely when `deliveryTargetMode === 'current'` — current-location mode is a one-shot, not a saved address. If a customer wants their actual current location saved they should use the existing `AddressEditScreen` "📍 Use my current location" button which only stamps coords onto a form they're actively typing (no copy-from-Home failure mode). (2) when auto-save DOES fire (form mode, zero prior addresses), stamp a contextual default label (`addr.city.trim() || 'Home'`) so the address book starts with a distinguishable entry rather than an unlabelled row that renders as the generic "Address" fallback. `persist()` helper now takes an optional `label` arg, threaded through to `profileService.saveAddress` (which already accepts the field — back-compat). The "Save this address?" prompt path for priorCount > 0 stays as-is — the customer who's already curating a saved address book gets the existing yes/no prompt and can label via `AddressEditScreen` if they want. Pure client OTA: `eas update --branch production --message "PR-NEXT-ADDRESS-UX current location naming + smart defaults"`. No new tests — behavioral guard with no pure helper to extract; manual acceptance covers it (place an order with current-location → confirm no new saved address appears; place an order with form-entered fields and zero prior saves → confirm new address has a city-or-Home label, not blank). tsc + 1225 / 1225 unit tests still clean. Deferred (per prompt §B-§D, intentional scope cut): iOS `Alert.prompt` + Android name-prompt modal for naming new auto-saved addresses (over-engineering for the actual symptom — the city-fallback label + skip-on-current covers the Sudhir repro), reverse-geocoding via `expo-location.reverseGeocodeAsync` to populate line1/city/pincode from coords, multi-address picker in name prompt, maps preview during naming.
- **Workaround until fix shipped:** customer cleaned up duplicate "Address" entries in their address book manually after each current-location order.

---

## Retest 2026-06-01 — three reopens

Sudhir reran the bundle (HOTFIX-6, NOTIFY-EXTEND, PARTNER-VIS, PARTNER-CARD, COD-UX, ADDRESS-UX) end-to-end on iOS + Android. Most cases passed; three reopened. NOTIFY-EXTEND, PARTNER-VIS, COD-UX, HOTFIX-2, HOTFIX-5, ENH-1, ENH-2, ENH-3, PR-NEXT-13a, PR-NEXT-13d, Finding #17 all verified working.

### #19 retest — HOTFIX-6 partial (Cart still shows ₹25)

- **Symptom:** Shop menu page shows ₹100 (correct), `CartScreen` "Bill Details" section shows ₹25 (the flat fallback), checkout shows ₹100 again. The ₹25 → ₹100 mismatch moved one screen instead of being eliminated.
- **Root cause:** HOTFIX-6 patched `ShopCard` + `ShopDetailScreen`, but `CartScreen` reads `useCartStore(s => s.deliveryFee)` directly. The cart store snapshots `shop.deliveryFee` + `shop.deliveryChargeTiers` at add-to-cart time but does NOT snapshot `shop.location`, so the cart has no distance reference to feed `displayDeliveryCharge`. It falls through to the flat fallback.
- **Status:** ✅ **SHIPPED in PR-NEXT-HOTFIX-6.1** (June 1 2026). `useCartStore` (`@c:\Users\dahiy\grocery-mvp\src\store\useCartStore.ts:23-33,67-69,106-108,169-173,227-228,262-263,278-279,293-294,315-320,356-361`) gained an optional `shopLocation: { lat; lng } | null` field, snapshotted from `shop.location` in all three add-paths (`forceAddItem`, `forceAddMenuItem`, `replaceCartWithItems`), cleared on every empty-out branch (`decrement → 0`, `removeItem → 0`, `clearCart`), and persisted via `partialize` so a relaunch mid-cart keeps the geo reference. Legacy persisted carts (pre-this-PR) hydrate with the initial-state `null` and the helper falls through to the flat fee — same display as today's broken behaviour, no crash. `CartScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\CartScreen.tsx:12-21,28-35,41-65,115-123`) now reads `deliveryChargeTiers + shopLocation + useLocationStore.location`, computes `previewDeliveryCharge = displayDeliveryCharge(snapshot, customerLocation)`, and renders BOTH the Bill Details Delivery fee row AND the "To pay" / CTA total off it — so the cart screen's bill is internally consistent and matches what ShopCard / ShopDetail / CheckoutScreen show. `displayDeliveryCharge`'s static signature was relaxed (`@c:\Users\dahiy\grocery-mvp\src\utils\displayDeliveryCharge.ts:38-52`) from `Pick<Shop, ...>` to a structural shape with nullable `location` + `deliveryChargeTiers` — runtime already handled both via the existing guards, this just aligns static types with what the body already does. Two new helper tests pin the cart-snapshot shape (tier path via haversine + null/null legacy fallback). Suite at **1227 / 1227** (was 1225). Pure client OTA: `eas update --branch production --message "PR-NEXT-HOTFIX-6.1 CartScreen Bill Details uses tier charge"`. Deferred (explicit out-of-scope): migrating `useCartStore.total()` itself to the tiered charge (would ripple into placeOrder's local-balance check + Analytics; kept the inline `subtotal + previewDeliveryCharge` in CartScreen and left other call sites on the legacy sum); a distance-aware "≈ ₹X · 3.2 km" subline.

### #22 retest #2 — PARTNER-CARD.1 partial (placeholders, not meaningful)

- **Symptom:** Sudhir's June 1 evening retest of PARTNER-CARD.1 — *"are we showing meaningful information here or these are just placeholders? It should give some live tracking options, delivery partner other details that can help customer etc. So please design solution that is really meaningful and helpful."* AND a silent bug: customer phone reveal always failed with `not_customer` even on the customer's own orders.
- **Root cause (UX):** PARTNER-CARD.1 added the rows the data happened to have (`#order`, `drop distance`, `drive time`) instead of asking *"what does a worried customer with hot food on the way actually want to know?"* The static-at-order-placement values went stale the moment the partner started moving — they're useful as context but not as "live tracking."
- **Root cause (bug):** PARTNER-CARD.1's `getDeliveryPartnerContactPure` (`@c:\Users\dahiy\grocery-mvp\functions\src\partnerContactHelpers.ts:90`) gated on `order.customerId`, which is not a real order field. Server orders are written with `customerUid` (10+ confirmed sites in `functions/src/index.ts`). The strict-equality check always returned false against `auth.uid`, so the callable always errored `permission-denied`. Tests passed because the fixture ALSO used `customerId` — a textbook self-confirming schema mismatch. New **Rule 5 (schema verification)** added to `.windsurf/code-discipline.md` requires every prompt that references doc fields to include an audit-grep table.
- **Status:** ✅ **SHIPPED in PR-NEXT-PARTNER-CARD.2** (June 1 2026 night). Six-part redesign answering the customer's five questions (who / where / when / how to reach / what they look like):
  - **(1) Bug fix** — `@c:\Users\dahiy\grocery-mvp\functions\src\partnerContactHelpers.ts:85-92` flipped to `customerUid`, with a paragraph-long comment explaining why this was wrong before and pointing at the audit-grep that catches future occurrences. Fixtures updated in `@c:\Users\dahiy\grocery-mvp\tests\functions\getDeliveryPartnerContactHelpers.test.ts`.
  - **(2) Pure helper `formatLivePartnerEta`** (`@c:\Users\dahiy\grocery-mvp\src\utils\formatLivePartnerEta.ts`) — owns all edge-case copy for the WHEN + Distance rows: `<1 min → "Arriving now" / "Almost there"`, `<50 m → hide distance row (WHEN carries it)`, `>60 min → "X.X hr"`, stale → `~ estimated` suffix. 10 tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\formatLivePartnerEta.test.ts`.
  - **(3) Pure helper `formatPartnerTrust`** (`@c:\Users\dahiy\grocery-mvp\src\utils\formatPartnerTrust.ts`) — WHO line + vehicle icon. Maps `(rating, count, vehicleType) → ("⭐ 4.8 · 142 deliveries", 🛵)` with `"⭐ New partner · welcome them!"` fallback when `ratingCount <= 0` (avoids the broken-looking "⭐ 0.0 · 0 deliveries"). 7 tests pin the four vehicle glyphs + new-partner branch + singular "1 delivery."
  - **(4) Denormalize helper + claimDelivery wiring** — `denormalizePartnerTrust` (`@c:\Users\dahiy\grocery-mvp\functions\src\claimDeliveryHelpers.ts:51-89`) extracts `(rating, deliveriesCount, vehicleType)` from the partner's `users/{uid}` doc with validation (whitelist + `Number.isFinite` + `Math.floor`). `claimDelivery` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:3668-3700`) stamps all three onto the order doc alongside the existing `deliveryPersonName` denormalization. 5 tests cover full data, missing fields, out-of-whitelist vehicle, non-finite numbers, null/undefined input. Optional + nullable shape on `Order` (`@c:\Users\dahiy\grocery-mvp\src\types\index.ts:544-562`) so legacy claimed-pre-PR orders render via the "New partner" fallback.
  - **(5) Live ETA callable `getLivePartnerEta`** — pure helper `@c:\Users\dahiy\grocery-mvp\functions\src\livePartnerEtaHelpers.ts` (haversine + `AVG_URBAN_KMH = 20` constant, `STALE_AFTER_MS = 2min`, `.toMillis()`-narrowing for Rule 12). Pre-pickup leg targets `shopLocation`, post-pickup targets `deliveryLocation`. Both fields nullable so legacy pre-PR-49 orders fail soft (`no_target_location` → `failed-precondition`) and the client falls back to static. Callable registered at `@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:4353-4391`. 10 tests pin all 5 failure codes + happy path + stale flag + Timestamp narrowing.
  - **(6) Client surface** — `orderService.getLivePartnerEta` wrapper (`@c:\Users\dahiy\grocery-mvp\src\services\orderService.ts:1672-1694`), polling hook `useLivePartnerEta` (`@c:\Users\dahiy\grocery-mvp\src\hooks\useLivePartnerEta.ts`) at 30s cadence with auto-pause on `enabled=false` (sheet dismissed → no callable hits, no battery drain) and React-18-strict-mode-safe cancellation ref. Redesigned `PartnerDetailsSheet` (`@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerDetailsSheet.tsx`) on `BottomSheet` chrome (HOTFIX-7), 3-tier fallback ladder (live → static → em-dash), 3-branch phone disclosure (pre-pickup muted / post-pickup tap-to-call as a primary block button / post-pickup reveal CTA when phone not yet cached). Wired in `OrderDetailScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\OrderDetailScreen.tsx:29-34,139-145,950-971`) — hook's `enabled` arg is `partnerSheetOpen` so polling lifecycle tracks sheet visibility.
  - **Deferred (§I, explicit out-of-scope):** partner-side `vehicleType` picker UI + one-time backfill script. Existing partners without `vehicleType` claim cleanly via the helper's null path and render with the motorbike default glyph. Future PR adds the profile-screen picker once we have a signal that customers are noticing.
  - **Suite at 1274 / 1274** (was 1241; +33 — exactly the prompt's forecast). `tsc --noEmit` clean across `src/` and `functions/`. Server-first deploy: `firebase deploy --only "functions:getLivePartnerEta,functions:getDeliveryPartnerContact,functions:claimDelivery"` → IAM verify all 3 (Rule 11) → `eas update --branch production --message "PR-NEXT-PARTNER-CARD.2 live ETA + trust signals + customerUid fix"`.

### #22 retest — PARTNER-CARD partial (sheet too thin)

- **Symptom:** Screenshot shows the sheet open with only avatar + name + "On the way to you" + "Picking up from: US Shoppers" + Close. Post-pickup the phone row hides (correctly, since the pre-pickup copy doesn't apply) but nothing replaces it.
- **Root cause:** Shipped sheet was deliberately restrained — phone reveal punted, ride info not surfaced, order id not shown. Restraint was correct for v1 but Sudhir's gut is right that this is too empty to justify the tap.
- **Status:** ✅ **SHIPPED in PR-NEXT-PARTNER-CARD.1** (June 1 2026). Three additions, no order-doc schema change. (1) `PartnerDetailsSheet` (`@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerDetailsSheet.tsx:23-31,38-91,135-219,313-329`) extended with six new optional props (`orderShortId`, `deliveryDistanceKm`, `deliveryDurationMin`, `partnerPhone`, `onRevealPhone`, `revealing`). Renders an Order row (`#ABCD1234`), Drop distance row, and a duration row whose label flips between "Drive time" pre-pickup and "Arriving in" post-pickup — each independently gated so pre-PR-46 orders without those fields hide the row cleanly rather than rendering `NaN km`. Phone is now a 3-branch render: pre-pickup → muted "shared after pickup"; post-pickup with phone cached → tap-to-call `tel:` link; post-pickup without phone → "Show partner phone" CTA. (2) New server-gated callable `getDeliveryPartnerContact` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:4241-4306`) backed by a pure helper `@c:\Users\dahiy\grocery-mvp\functions\src\partnerContactHelpers.ts` with Validator-Result discriminated-union pattern matching the codebase's existing `codPaymentHelpers` / `claimDeliveryHelpers` posture. Five failure codes (`order_not_found`, `not_customer`, `no_partner`, `not_picked_up`, `no_phone_on_partner`) plus the happy path are pinned by 9 unit tests in `@c:\Users\dahiy\grocery-mvp\tests\functions\getDeliveryPartnerContactHelpers.test.ts` — covers the core privacy gate (pre-pickup must fail closed) plus defensive cases (empty `deliveryPersonId`, missing `pickedUpAt` field entirely, empty `phoneNumber`). Phone is NEVER denormalized onto the order doc — the only surface is this explicit pull, so customer fetch of `getOrder` / Firestore listener can't leak it. (3) Client wrapper `orderService.getDeliveryPartnerContact` (`@c:\Users\dahiy\grocery-mvp\src\services\orderService.ts:1629-1651`) with native + web paths. `OrderDetailScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\OrderDetailScreen.tsx:127-133,249-283,939-952`) added `partnerPhone` + `revealingPhone` useState, `onRevealPartnerPhone` useCallback (calls the callable, caches phone, surfaces friendly error via existing `showAlert`), an `orderId`-keyed reset useEffect (clears cached phone when navigating between orders), and threads the new props into the sheet. Suite at **1236 / 1236** (was 1227, +9). Server-first deploy required (Rule 11 IAM verify after Cloud Run service creation): `firebase deploy --only "functions:getDeliveryPartnerContact"` → `gcloud run services get-iam-policy getdeliverypartnercontact --region asia-south1` (add `allUsers / roles/run.invoker` if missing) → `eas update --branch production --message "PR-NEXT-PARTNER-CARD.1 richer sheet + phone reveal"`. Deferred: partner rating display, live partner-location ETA polling, profile photo, admin/shop-owner contact reveal.

### #24 retest — ADDRESS-UX partial (no way to name a current-location address)

- **Symptom:** Sudhir's words — *"Are we going to use that exact location for final delivery? I wanted to give an option to user to save that address with some meaningful name like Office address, or Sector 10 Home, Or uncle Home so they can use it next time."* The shipped behavior skips save entirely on current-location, but the customer wants the OPTION to save it with a name.
- **Root cause:** ADDRESS-UX's §B (`Alert.prompt` + Android name modal) was cut by Windsurf as over-engineering. The minimal fix (skip auto-save + city-fallback label for form-mode) covered the auto-creation symptom but not Sudhir's intent of building a re-usable address library from current-location orders.
- **Status:** ✅ **SHIPPED in PR-NEXT-ADDRESS-UX.1** (June 1 2026). New `SaveCurrentLocationModal` (`@c:\Users\dahiy\grocery-mvp\src\components\address\SaveCurrentLocationModal.tsx`) — raw `Modal` + backdrop slide-up matching `ReorderModal` / `PartnerDetailsSheet` convention (no `@gorhom/bottom-sheet` dependency). Pre-fills name field + Home/Office/Other chips + optional street/city/pincode rows from reverse-geocode. Client-side save gate mirrors the server's `validateAddressInput` requireds (`line1`, `city`, 6-digit pincode) so the Save button only enables when the round-trip will succeed — no silent server rejection if reverse-geocode returned partial data; customer fills the blanks first. Empty-label fallback persists the geocoded default rather than a blank "Address" row (the original Sudhir complaint). New `reverseGeocodeLabel` helper (`@c:\Users\dahiy\grocery-mvp\src\utils\reverseGeocodeLabel.ts`) wraps `Location.reverseGeocodeAsync` with try/catch + a pure `buildGeocodeSuggestion` projection that handles all-null inputs, district-vs-city deduplication (avoids "Bengaluru, Bengaluru" labels), null pincode → empty string coercion. 5 unit tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\reverseGeocodeLabel.test.ts` pin the projection across happy path / undefined input / missing district / all-null / null pincode (expo-location mocked at module-resolver level so the package's ESM doesn't fight Jest's CommonJS pipeline). `CheckoutScreen` (`@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx:35-47,165-187,514-564,1258-1320`) wired: new `saveLocationModalVisible` + `geocodeSuggestion` + `pendingSaveCoords` useStates + `saveLocationPromiseRef` so `maybeSaveAddressAfterOrder` AWAITS the modal dismissal (Save or Skip both resolve the promise) before `placeOrder` proceeds to `nav.replace('OrderConfirmation', …)` — critical because opening the modal is just `setState` and without the ref the navigation would unmount CheckoutScreen and take the modal with it before the customer could pick. Current-location branch now snapshots `(lat, lng, name, phone)` BEFORE the modal opens so the persist call is independent of any still-mounted form state. Save fires `profileService.saveAddress` with the chosen label + reverse-geocoded fields + the locked coords; failure is swallowed to `console.warn` (order is already placed, missing saved-address sync isn't worth a hard alert mid-confirmation). Skip → resolves promise without saving (same posture as the pre-modal silent-skip). Pre-existing form-mode auto-save paths (priorCount === 0 city-fallback, priorCount > 0 yes/no alert) unchanged — no regression. Suite at **1241 / 1241** (was 1236, +5). Pure client OTA — `expo-location` already in deps, no new permissions: `eas update --branch production --message "PR-NEXT-ADDRESS-UX.1 save-current-location prompt + reverse-geocode"`. Deferred: maps preview of the saved pin during naming, Google Places "nearby business" auto-name (API budget), edit-in-place after Save (customer edits via existing Address book), migration of historical current-location orders to retro-create address rows.

### June 1 evening retest — HOTFIX-7 + two ADDRESS-UX.1 follow-ups

Three bugs surfaced together; all shipped in one batch.

**Bug 1 — Android gesture-nav clipping on bottom-anchored modals** (Sudhir: *"Whenever we add anything new in the app, this issue always comes. Can we make sure fix is applied at first place instead of applying a fix. It wasted so much time."*). `SaveCurrentLocationModal`'s Save CTA clipped by the gesture pill in the retest screenshot. Same bug class HOTFIX-3 hit on `CartScreen`. Fixed structurally in **PR-NEXT-HOTFIX-7**:
  - New `@c:\Users\dahiy\grocery-mvp\src\components\common\BottomSheet.tsx` — single source of truth for bottom-anchored modal chrome. Uses `useSafeAreaInsets().bottom + spacing.lg` for padding (vs hand-coded `spacing.xl` / `spacing.xxl` that under-padded gesture-nav and over-padded 3-button-mode). Supports `keyboardAvoid`, `showHandle`, and `onBackdropPress` (escape hatch for `CancelAndRefundModal`'s keyboard-dismiss-only posture).
  - Migrated three modals: `@c:\Users\dahiy\grocery-mvp\src\components\address\SaveCurrentLocationModal.tsx`, `@c:\Users\dahiy\grocery-mvp\src\components\order\PartnerDetailsSheet.tsx`, `@c:\Users\dahiy\grocery-mvp\src\components\order\CancelAndRefundModal.tsx`. Each strips the hand-rolled `Modal` + backdrop + `KeyboardAvoidingView` + handle + dead style entries; wraps content in `<BottomSheet>`.
  - **Rule 13** added to `@c:\Users\dahiy\grocery-mvp\.windsurf\code-discipline.md:360-407` — bottom-anchored modals MUST go through `BottomSheet` (or use `useSafeAreaInsets` if they can't). Includes the audit-grep and the API table.
  - **Deferred (explicit out-of-scope):** four admin screens (`DeliveryRequestDetailScreen`, `ShopDetailManagementScreen`, `ShopRegistrationDetailScreen`, `UserDetailScreen`) also have hand-rolled bottom-anchored modals. Flagged in Rule 13's audit-grep exception list. Admin surfaces have a lower bug bar than the customer-facing three; separate migration when convenient.

**Bug 2 — Shopkeeper sees wrong delivery address on current-location orders** (Sudhir: *"on shopkeeper side, on order detail, Delivery address is showed as one of my saved dummy address. That is for sure wrong."*). Root cause: when the customer has a default saved address auto-selected on Checkout (which pre-fills form fields via `applySavedToForm`), then taps "Deliver to current location," the form fields STILL hold the stale Home data. `placeOrder` was building the order's `address` payload from those stale form fields, so the shopkeeper saw Home text while `deliveryLocation` correctly held the GPS pin. Two-pronged fix:
  - **Root cause:** `@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx:487-505,680-761` — `validate()` no longer requires `line1`/`city`/`pincode` in current-location mode (form fields can be stale), and `placeOrder` now reverse-geocodes the live coords BEFORE submission and overrides `line1`/`line2`/`city`/`pincode` with geocoded values + a `📍` marker + a `(lat, lng)` coords-as-line2 fallback. Sentinel "—" / "000000" pass server validation (`/^\d{6}$/`) when reverse-geocode returns nothing — clearly placeholder values to the shopkeeper rather than the wrong-city fields. Form pincode is intentionally NOT used as fallback (that's the bug we're fixing).
  - **Defense in depth:** `@c:\Users\dahiy\grocery-mvp\src\screens\shop\ShopOrderDetailScreen.tsx:294-331,654-675` — when `order.deliveryLocation?.type === 'current_location'`, render a prominent `primaryLight`-tinted GPS-pin banner above the address card with the coords + a one-tap maps deeplink (iOS `maps:`, Android `geo:`, web Google Maps URL). On legacy pre-HOTFIX-8 orders (where `deliveryAddress` still has stale Home text) the banner is the visible truth; on modern orders the geocoded address text reinforces it.

**Bug 3 — `SaveCurrentLocationModal` Save button disabled with no explanation** (Sudhir: *"I got option to save the address while used default location option but button was disabled as well."*). Root cause: client-side gate required `line1` + `city` + 6-digit pincode (to mirror server's `validateAddressInput`), but if reverse-geocode returned partial data the customer just saw a dim button. Fix: `@c:\Users\dahiy\grocery-mvp\src\components\address\SaveCurrentLocationModal.tsx:98-125,165-179,275-279` — inline warning hint listing the missing/invalid fields ("Add street/area and 6-digit pincode to enable Save."), with the `Street / Area` placeholder rewritten from misleading "(optional)" to actionable "House / building, street, locality" (it's required for save, the prior placeholder actively misled).

**Status:** ✅ ALL THREE SHIPPED in **PR-NEXT-HOTFIX-7 + PR-NEXT-HOTFIX-8** (June 1 2026 evening). Suite at **1241 / 1241** (unchanged from afternoon — presentational migration + behavior fixes covered by acceptance, not unit tests). `tsc --noEmit` clean across both `src/` and `functions/`. Pure client OTA: `eas update --branch production --message "HOTFIX-7 BottomSheet + HOTFIX-8 current-location address truth"`.

### Push notification clarification (not a bug)

Sudhir's side question: *"delivery person getting notification after shop mark it ready for pickup. but not for first 2 actions. so that means push notification is tied to the functions? and for first 2 functions it is having some configuration or access issue?"*

This is by design, not a config issue:

- `acceptOrder` and `markPreparing` are shop → customer transitions. Delivery partners aren't on the recipient list because no partner is assigned yet — the partner pool only becomes interested at `ready_for_pickup` (the radius-filtered broadcast in PR 50).
- `markReadyForPickup` fans out to in-radius delivery partners (PR 50). That's the first time the partner is "on the audience list."
- After a partner claims (`claimDelivery`), subsequent transitions (`markPickedUp`, `markDelivered`) target the claiming partner + shop + admin + customer per the audience-precedence convention. So the partner does get push for those.

Net: pushes are wired correctly for the partner role. Confusion is intentional opacity, not missing config. (Separately, the customer-side Android FCM `InvalidCredentials` issue is still parked — operational fix path is clear-data-then-reinstall, then rebuild Android if that fails.)

---

## Enhancements / nice-to-haves

(See #5, #6, #9, #13 above — each tagged with the cluster it belongs to.)

---

## How to add a finding

For each new bug or enhancement, copy the template:

```
### #N — short title

- **Severity:** Pilot-blocker / High / Medium / Low
- **Platform:** iOS / Android / Both
- **Symptom:** what the tester sees
- **Root cause:** *(fill in after diagnosis; OK to leave "TBD")*
- **Scope:** files / surfaces affected
- **Fix approach:** brief direction
- **Status:** Logged / Diagnosed / Prompt drafted / In progress /
  Shipped / Verified
- **Workaround:** if any
```

Keep entries terse — this file is for tracking, not documenting.
Detailed diagnostic notes belong in the Windsurf prompt for the fix,
not here.

### June 2 retest — two HOTFIX-8 / ADDRESS-UX.1 follow-ups

Two new findings from Sudhir's June 2 morning testing of the just-shipped HOTFIX-8 + ADDRESS-UX.1 cluster. Both shipped in one batch.

#### Bug 2 reintroduction race window — Place Order tappable during GPS capture

- **Symptom:** Sudhir's June 2 — *"Place order button was active when I clicked on use current location for delivery, and it was still calculating the location point. I think when we use current location, place order button should be disabled until final location is calculated, otherwise it is picking default saved home location even customer is picking current location as an option."*
- **Root cause:** HOTFIX-8 fixed Bug 2 (the wrong-address-stamped-on-order bug) AT placeOrder time, but only along the path where `liveCoords` are present. The fix relaxed `validate()` so form-stale fields don't block submit in current-location mode — which exposed the race window. Customer taps "Deliver to current location" → `capturingLive=true`, GPS resolves async (1-3s typical) → during that window the CTA is still enabled because `disabled={placing}` is the only gate. A fast tap there ships an order with no `deliveryLocation` (because `resolveDeliveryLocation` returns `null` when `liveCoords` missing) AND falls through HOTFIX-8's reverse-geocode branch (which also requires `liveCoords`), landing in the legacy form-stamp path. Bug 2 returns.
- **Status:** ✅ **SHIPPED in PR-NEXT-HOTFIX-9** (June 2 2026). Pure client OTA, no callable, no schema. Four-part fix in `@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx`:
  - **Derived flag** at line 415-437 — `blockingOnCurrentCapture = deliveryTargetMode === 'current' && (capturingLive || !liveCoords)` AND `canPlaceOrder = !placing && !blockingOnCurrentCapture`. Sits with the other derived flags above any conditional return (Rule 2). Saved-mode and form-mode (`deliveryTargetMode === null`) skip the gate entirely so existing flows are untouched.
  - **CTA wired** at line 1376-1388 — `disabled={!canPlaceOrder}` on the single Place Order / Pay button. The pre-existing `loading={placing}` stays as the spinner driver during the network round-trip; `canPlaceOrder` adds the GPS-capture gate on top.
  - **Inline hint** at line 1369-1375 — `📍 Capturing your location…` while `capturingLive` is in flight, flips to `⚠️ Couldn't get your location. Tap "Deliver to current location" again to retry.` if `liveCoordsError` populated. Customer-driven retry is intentional — auto-retry can mask permission denials and feels invisible.
  - **Defensive in-function guard** at line 679-692 — re-checks `deliveryTargetMode === 'current' && (!liveCoords || capturingLive)` at the top of `placeOrder` (after the auth gate, before `validate()`). Logs `console.warn('[Checkout] placeOrder fired during GPS capture; ignoring.')` and returns. Belt + suspenders so a future refactor (e.g. someone loosens the disable, or the existing `usePressGuard` releases right as state flips) can't re-expose Bug 2 from below the UI. Rule 1's spirit: button-disable is the user-facing fix, the in-function check is the structural lock.
  - Pure UI state guard, no test count change. Suite at **1274 / 1274**. `tsc --noEmit` clean.

#### Duplicate addresses on repeat-current-location orders

- **Symptom:** Sudhir's June 2 — *"Placed 2 orders from same location and used current location option as delivery address, it saved exact 2 addresses in the profile. I think we should not save duplicated addresses. Or if latitude longitude are same, no need to popup to save duplicate address. Or something else, but creating duplicate address is not good."*
- **Root cause:** ADDRESS-UX.1 wired the post-order "Save this location?" modal but had no dedupe — every successful current-location order opened the modal, every Save persisted a fresh row even when the GPS pin was within meters of an already-saved address. The customer's address book accumulated noise instead of becoming the reusable library Sudhir wanted in the original ADDRESS-UX intent.
- **Status:** ✅ **SHIPPED in PR-NEXT-HOTFIX-10** (June 2 2026). Pure client OTA. Decision (locked pre-design): silent skip — modal doesn't appear when an existing address pins within 25m haversine of the GPS reading; small toast confirms which one matched. 25m threshold tuned to typical urban GPS accuracy (5-20m outdoor / 30-50m indoor) — collapses same-building re-orders without merging next-door neighbours. Three-part build:
  - **Pure helper `findAddressNearby`** at `@c:\Users\dahiy\grocery-mvp\src\utils\findAddressNearby.ts`. Operates on `SavedAddress[]` (not `Address` — only the saved-side shape carries `lat/lng/label`). Defensive `Number.isFinite` filter on both target and candidate coords (legacy pre-PR-46 addresses with no pin are skipped, not treated as "far"). Returns the CLOSEST match within threshold (not just the first), so if a customer somehow has two pins within 25m of GPS, the toast confirms the more accurate one. Boundary INCLUSIVE (`distM <= thresholdM`) — matches `chargeForDistance` tier-boundary convention. 8 helper tests in `@c:\Users\dahiy\grocery-mvp\tests\utils\findAddressNearby.test.ts` covering exact match / 1m / 24.9m / 25.0m boundary / 30m (out) / no-coords / non-finite target / closest-of-many tiebreak.
  - **Toast primitive** at `@c:\Users\dahiy\grocery-mvp\src\components\common\Toast.tsx` — bare-minimum animated bottom-anchored notification. `useSafeAreaInsets().bottom + spacing.xl` so it floats above Android gesture-nav pills (Rule 13 from HOTFIX-7). `pointerEvents="none"` so it can never block the CTA below. 200ms fade in / 3s display / 200ms fade out then dismiss-callback fires. Single Toast per screen for now; if multi-toast queueing becomes a need swap to `react-native-root-toast` later.
  - **Wired in `CheckoutScreen`** at `@c:\Users\dahiy\grocery-mvp\src\screens\CheckoutScreen.tsx:40-50,191-196,584-604,1490-1502`. The `maybeSaveAddressAfterOrder` current-location branch now calls `findAddressNearby(profile?.addresses ?? [], liveCoords)` BEFORE setting `pendingSaveCoords`. On match → `setToastMessage('Saved as Home (already in your address book)') + setToastVisible(true) + return`, skipping the modal entirely. On no match → existing ADDRESS-UX.1 flow continues unchanged. Toast mounted at the SafeAreaView root after the SaveCurrentLocationModal slot.
  - Suite at **1282 / 1282** (was 1274; +8 exactly as forecast). `tsc --noEmit` clean. Deferred (explicit out-of-scope): bulk dedupe of pre-existing duplicate addresses (one-time script territory, not worth pilot-time migration risk); map-preview at modal-open time so the customer can visually confirm "yes that's me"; consolidation hint when the saved-address picker sees nearby duplicates pre-PR.
### June 2 observations #1+#2+#3 — radius / location-less shops cluster

- **Symptom:** Sudhir, June 2 — *"Shop current location is optional so how can we calculate shop distance?"* + the cascading symptoms (#2 customers see distant Faridabad shops despite a 5km service radius, #3 the radius gate appears to do nothing on certain shops, #4/#7 India team sees nothing). All trace back to one root: shops without a `location` field passing through the customer-facing distance filter as `distanceKm: undefined` → fail-OPEN → globally visible.
- **Root cause (3 failures, 3 layers, 1 PR):**
  1. **Client gate gap** — `RegisterShopScreen.validate()` did not require `location`. Owner could register a shop with no GPS pin.
  2. **Server gate gap** — `approveShop` only checked `status === 'pending'`. Admin could approve a location-less shop (intentionally, accidentally, or via direct callable invocation that bypassed the UI).
  3. **Filter gate gap** — `filterShopsByServiceRadius` was uniformly fail-OPEN on missing `distanceKm`. The original posture was correct for the customer-side gap (no GPS granted → don't strand them) but wrong for the shop-side gap (shop has no `location` → silently visible to everyone).
- **Status:** ✅ **SHIPPED in PR-NEXT-SHOP-LOCATION-REQUIRED** (June 2 2026). Defense in depth — three independently-sufficient layers + an audit-trail stamp + an operational pre-deploy script:
  - **Layer 1 (client gate)** — `@c:\Users\dahiy\grocery-mvp\src\screens\roles\RegisterShopScreen.tsx:166-189,701-726`. `validate()` now refuses submit when `location` is null with an actionable message pointing at the existing GPS-capture row. Continue button hard-disables (`disabled={submitting || !location}`) AND a yellow inline hint above it explains why. New `captureHint` style at line 981-990 mirrors HOTFIX-9's pattern from CheckoutScreen.
  - **Layer 2 (server gate)** — pure helper `validateShopLocationForApproval` at `@c:\Users\dahiy\grocery-mvp\functions\src\approveShopHelpers.ts:116-184`. Discriminated-union return shape (`{ ok: true } | { ok: false, code: 'no_location' | 'lat_invalid' | 'lat_out_of_range' | 'lng_invalid' | 'lng_out_of_range' }`) so the callable maps to specific error messages. Strict checks: lat finite + ∈ [-90,90], lng finite + ∈ [-180,180] (the range check catches a swapped lat/lng ship-it where Delhi's 28.6/77.2 ends up as 77.2/28.6 — 77.2 > 90 fails). Wired into `approveShop` at `@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:4931-4956` between the existing `status === 'pending'` check and the storefront-photo wiring. Server returns `failed-precondition` with the specific reason + an actionable "ask the owner to re-open RegisterShop" message. **12 unit tests** in `@c:\Users\dahiy\grocery-mvp\tests\functions\approveShopHelpers.test.ts:144-249` covering happy path / null / missing field / NaN / string / lat 91 / lat -91 / lng Infinity / lng 181 / boundary 90/180 inclusive / 0,0 (technically valid).
  - **Layer 3 (filter gate)** — `filterShopsByServiceRadius` signature now takes `customerHasLocation: boolean` so the missing-distance branch can split: customer-side gap → keep (preserve fail-OPEN; don't strand a customer without GPS); shop-side gap → drop (defense layer 3 — the location-less shop is misconfigured; hide it). Server helper at `@c:\Users\dahiy\grocery-mvp\functions\src\geoVisibilityHelpers.ts:13-89`, byte-identical client mirror at `@c:\Users\dahiy\grocery-mvp\src\utils\geoVisibilityHelpers.ts:14-48`. Both call sites updated: `listShopsPublic` (`@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:7160-7170`) passes `customerHasLocation: !!userLocation`; `shopService.getNearbyShops` web Plan B path (`@c:\Users\dahiy\grocery-mvp\src\services\shopService.ts:75-103`) passes `customerHasLocation: true` (the function REQUIRES `userLocation`) AND adds a defensive guard so `haversineKm` doesn't throw on a location-less shop — stamps `distanceKm: undefined` instead, which the new fail-CLOSED branch correctly drops. **22 unit tests** in `@c:\Users\dahiy\grocery-mvp\tests\functions\geoVisibilityHelpers.test.ts` covering all original cases (re-typed with `customerHasLocation: true`) + new customer-side-gap branch (3 tests) + new shop-side-gap branch (4 tests including a mixed-table case that drops only the location-less shop) + a customer-without-GPS uniform-keep test.
  - **Audit-trail stamp** — `Shop` type at `@c:\Users\dahiy\grocery-mvp\src\types\index.ts:107-116` gains `locationVerifiedAt?: number` + `locationVerifiedBy?: string`. Both optional + nullable so legacy approved-pre-PR shops stay back-compat (they simply lack the stamp). `approveShop` writes both alongside the existing `approvedAt`/`approvedBy` inside the `shopRef.update` block at `@c:\Users\dahiy\grocery-mvp\functions\src\index.ts:5055-5076`.
  - **Admin UI affordance** — `@c:\Users\dahiy\grocery-mvp\src\screens\admin\ShopRegistrationDetailScreen.tsx:81-91,169-190,407-455,624-658`. New `locationVerifiedChecked` useState (Rule 2: above conditional returns); derived `shopHasValidLocation` flag mirrors the server helper; derived `canApprove = !pending && shopHasValidLocation && locationVerifiedChecked`. Two render paths above the Approve CTA: (1) shop has no / invalid pin → red banner `"⚠️ No GPS location captured (or out of valid earth range). Cannot approve until the owner re-opens RegisterShop and captures a valid pin."` + Approve hard-disabled (no checkbox can unlock). (2) shop has a valid pin → tappable verification checkbox `"☐ I verified this shop's location on the map. (Tap the 📍 coords above to open Google Maps.)"` next to the existing PR-31.1 tappable coords row. Approve enables only when both `shopHasValidLocation` AND `locationVerifiedChecked` are true. Checkbox state is local — the durable record is the server-stamped `locationVerifiedAt`/`By`. New `locationBanner`, `verifyRow`, `verifyCheckbox`, `verifyLabel` styles.
  - **Operational pre-deploy script** — `@c:\Users\dahiy\grocery-mvp\scripts\audit-shops-without-location.ts`. Re-uses the same `validateShopLocationForApproval` helper to enumerate every shop in Firestore whose `location` is missing or out of range. Tab-separated output (shopId / status / name / ownerUid / reason) + a summary line + a WARNING line if any of the offenders are currently `status: 'active'` (those will become invisible to customers post-deploy). Run via `npx tsx scripts/audit-shops-without-location.ts`.
  - **Suite at 1299 / 1299** (was 1282; +17). `tsc --noEmit` clean for both `src/` and `functions/`.
  - **Deferred (explicit out-of-scope, §I):** map-based location editor in RegisterShop (drag pin); periodic re-verification of shop location; custom `serviceRadiusKm` at registration time; bulk-fixing existing location-less shops via code (handled operationally per §E — let them disappear, owners re-capture).