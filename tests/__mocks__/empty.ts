// Empty module stub for side-effect imports we don't care about in
// unit tests (e.g. `import '@react-native-firebase/functions'` adds
// the .functions() member onto the app object — we mock that
// member directly in rnfb-app.ts so this side-effect is a no-op).
export {};
